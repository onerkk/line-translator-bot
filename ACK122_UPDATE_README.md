# ACK122：依 @ 標記決定作業確認的追蹤對象

以 ACK121 為基礎，只提供本次變更檔案。將 ZIP 內所有檔案依原路徑覆蓋到儲存庫根目錄，保留 `static/` 與 `tests/` 目錄，再部署並重新啟動所有服務程序。

`line_factory_features.py`、`line_ack_reminders.py` 必須一起更新，版本皆為 `2026-09-09.ack-recipient-scope.7`。不要再執行舊更新包的安裝程式，以免覆蓋回舊檔案。

## 使用規則

`/確認`、`/ack`、`/确认` 都支援相同規則：

| 指令內容 | 追蹤範圍 |
| --- | --- |
| `/確認 請完成設備檢查` | 全群 |
| `/確認 @All 請完成設備檢查` | 全群 |
| `/確認 @甲 請完成設備檢查` | 只追蹤甲 |
| `/確認 @甲 @乙 請完成設備檢查` | 只追蹤甲、乙 |
| 同時標記個人與 `@All` | 以全群為準 |

指定個人時，請使用 LINE 的 `@` 選人功能。程式依原始標記的使用者 ID 識別對象，重複標記同一人只計算一次；不會根據譯文或姓名猜人。純文字 `@all`（不分大小寫）也可選擇全群追蹤；若實際標記的是名字叫 All 的某位成員，仍只追蹤該人。

LINE 的標記資料區分 `user` 與 `all`，且個人的 ID 可能因資料授權狀態而未提供。若指定標記缺少有效 ID，本次不建立通知，也不擴大成全群；會提示重新確認標記。[LINE 官方文件](https://developers.line.biz/en/reference/messaging-api/nojs/#text-message)

## 回覆、提醒與介面

- 指定通知的追蹤範圍固定儲存；後續成員加入、名單補查、重新啟動或重送 webhook，都不會擴大成全群。
- 發起人與本機器人不列入待回覆對象；已離群者不列入提醒。範圍外成員點按不記入該通知，也不發群組訊息。
- 第一次按了解與重複按下都保持靜默；排程時間到才廣播更新名單，並個別 @ 範圍內仍未回覆者。
- 指定成員未回覆為 0 時自動停止。手動停止後補答仍可記錄，但不恢復提醒。
- 全群模式沿用可取得的群組名單與已辨識成員；未取得完整名單時保留提示，已知未回覆為 0 時仍按既定規則停止。
- 卡片、後台與 `/help`／`/說明` 同步說明追蹤規則。後台資源網址已更新，讓手機載入本版介面。
- 一併修正通知資料復原後可能立即發出提醒的問題：收到有效回覆後先確認通知已送達，再等待設定的提醒間隔。

新範圍規則適用於更新後建立的通知。已存在且未儲存範圍欄位的舊通知保留原有全群追蹤，不會重新解讀歷史標記或更動既有回覆、停止狀態。

## 驗證

- 完整後端回歸：2,177 passed、443 subtests passed。
- 介面檢查通過：包含指定範圍、無關成員點擊、指定成員完成、自動停止，以及原有頁面生命週期、設定、表單與快捷鍵流程。
- 翻譯資產檢查通過：0 errors、0 warnings。
- 測試涵蓋 SQLite／Redis、LINE SDK 標記物件、中英文指令、emoji 前綴位置、@All 優先、重複標記、發起人／機器人排除、名單補查、重啟與資料復原。

上述為本機測試；未向正式 LINE 群組發訊息，尚未部署到你的伺服器。本包的 `app.py` 只修改後台 JavaScript 資源版本網址；翻譯流程未變更。

`ACK122_SHA256SUMS.txt` 提供本包各檔案的 SHA-256 校驗碼。
