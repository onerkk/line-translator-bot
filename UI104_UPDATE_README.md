# UI104 更新說明

本包以 CI102 修復版為基礎，包含這次變更與新增的檔案，路徑直接對應 Repository 根目錄。

## 更新方式

1. 將 ZIP 全部解壓，依原資料夾路徑覆蓋 Repository，包含新增的 Python 模組、CSS 與測試檔。
2. `app.py` 位於根目錄；前端檔案位於 `static/`，模板位於 `templates/`，介面測試位於 `tests/`。
3. 一次提交整包更新。GitHub Actions 的 Factory translation release gate 通過後，再部署並重新開啟 LINE 內的網頁。
4. 包內 `UI104_FILES.sha256` 可用 `sha256sum -c UI104_FILES.sha256` 驗證檔案是否完整。

本次沒有執行正式部署，也沒有連線傳送 LINE 訊息或呼叫付費 AI。

## 介面與使用方式

- **作業確認以內容為主**：頂部僅保留小型標題、編號與發起人。通知原文以大字顯示，譯文獨立成區；回覆結果放在內容之後。
- **了解為主要按鈕**：「查看回覆」與「停止提醒」放在次要操作列。預設不提供「不了解／需說明」按鈕；舊紀錄仍保留，既有自訂按鈕設定仍可調整。
- **確認只由指令建立**：`/ack 內容` 與 `/確認 內容` 效果相同。一般翻譯不會自動新增確認卡。
- **提醒維持可控**：後台可設定間隔、是否持續提醒、整個群組關閉，亦可停止單筆通知。每輪重新排除已回覆者；LINE 名單不足時的 @All 會通知全群，不能保證僅觸及未回覆者。了解仍不代表作業完成。
- **完整分類 help**：分成日常翻譯、作業確認、工廠查詢、媒體、譯文工具、品質、設定、人名與管理診斷。每個指令列出用法、範例、群組／私訊與權限；可切換中文、印尼文說明。
- **新版手機與後台介面**：管理後台、群組設定、工廠工具、作業確認紀錄與表單共用一致的色彩、間距、欄位與按鈕樣式。內容較長時可換行，支援鍵盤焦點與手機放大。

## 已修正的問題

- `/wrongstats` 被 `/wrong` 的前綴判斷攔截，誤走錯誤標記流程。
- 已知指令的中文別名、參數與權限判斷不一致；現在共用同一份指令目錄。未知的斜線開頭文字仍進入翻譯流程。
- `/status` 把圖片「詢問」模式顯示成自動開啟，並固定顯示中印互譯，未反映實際目標語言。
- 指定翻譯 ID 修正時，缺少目前群組的範圍檢查。
- 多位管理員抽檢時，共用最後一批樣本而互相覆蓋；現在依使用者保存，評分樣本一小時有效。
- 工廠工具在使用者修改原文、語言或站別後，仍可能顯示／分享先前請求的譯文。
- 工廠與表單頁離開、返回時，舊請求仍更新頁面或拋出未處理錯誤。新版保留頁面生命週期防護，正常使用中的錯誤仍會顯示。
- 表單更換主題後的灰字與舊深色輸入框造成對比不足；已修正標籤、輸入框、勾選區與送出中狀態。
- 新／舊群組按鈕預設值意外啟用需說明按鈕。
- CI 增加新樣式與新介面測試檔的觸發路徑；本包的前端與介面測試相互匹配。

## 翻譯效能與品質

本次不降低模型等級、不縮減提示詞，也不移除數字、否定、操作先後順序或最終譯文檢查。原有的失敗重試與持久化傳送流程仍保留。

優化集中於不改變判定結果的重複工作：

- 工廠原因代碼的規則文字建立索引重用；規則內容變更時重新建立。
- 同一筆翻譯內重用相同原文與語言的 PMI 分析；不同請求不共用，回傳副本避免互相修改。
- 新群組的群名查詢移至有併發上限的背景工作，不在翻譯開始前等待這筆網路查詢。

離線效能測試使用固定 AI 與 LINE 回應、封鎖外部網路，每個情境取 7 筆中位數。以下是同環境依序執行的最後一輪結果，包含本機 SQLite 與檔案處理，不含真正的雲端或 LINE 傳送延遲。

| 情境 | 翻譯快取 | 更新前 ms | 更新後 ms |
|---|---|---:|---:|
| 日常短句 | 未命中 | 54.087 | 64.390 |
| 日常短句 | 命中 | 33.602 | 45.431 |
| 工廠術語短句 | 未命中 | 51.459 | 68.008 |
| 工廠術語短句 | 命中 | 36.486 | 46.428 |
| 多步驟工廠通知 | 未命中 | 88.140 | 94.819 |
| 多步驟工廠通知 | 命中 | 57.037 | 63.630 |

這輪總耗時較長，尚未證明整體回覆速度穩定提升，不能換算成正式 LINE 加速比例。逐函式檢查確認同一通知的規則正規化呼叫從 7,361 次降為 83 次，PMI 分析本體從 27 次降為 2 次；六種情境的輸出、模型與提示詞完全一致。實際回覆時間仍須部署後量測。

## 驗證結果

- 完整離線 Python 回歸：**1,920 passed，443 subtests passed**。
- 四組介面測試全部通過：後台頁面生命週期、成員／表單生命週期、工廠完整操作、統一快捷選單。
- 提醒測試涵蓋名單不足時原生 @All、重複排程、停止單筆、整群關閉與成員名單。
- 純資料資產檢查：無錯誤、無警告；四個前端 JS 語法檢查通過。
- Chromium 檢查：390px 與 1280px 的工廠後台、工具與表單無水平溢出、無 JavaScript 頁面錯誤；另檢視管理總覽與群組設定模板。
- 新 help 與確認卡通過 SDK 解析及內容大小／按鈕限制測試，並檢視本機 Flex 排版預覽。這不是正式 LINE 客戶端實機驗收。

本次測試可以驗證所涵蓋情境，無法由離線結果保證所有自由輸入都正確、第三方 API 永不故障，或完全沒有未知問題。

可在安裝專案測試相依套件後重跑：

```bash
python tests/run_factory_ui.py --check-assets
python validate_factory_translation_assets.py --json
python run_translation_offline_checks.py
# 先安裝 jsdom@30.0.1，JSDOM_PATH 指向該 jsdom 套件目錄
python tests/run_factory_ui.py
```

## 指令完整對照

英文、中文名稱共用相同功能與權限。既有別名（例如 `/saran`、`/absen`、`/stats wrong`）仍保留；`/suggest`、`/leave` 是對應的英文入口。圖片／語音／工單參數另支援「開啟、關閉」，圖片也支援「詢問」。

### 日常翻譯

| 英文 | 中文 | 使用範圍 | 說明與範例 |
|---|---|---|---|
| `/help` | `/說明` | 群組／私訊 | 分類查看所有指令；可接分類或指令名稱。 範例：`/說明 確認` |
| `/mylang` | `/我的語言` | 群組／私訊 | 選擇自己的閱讀語言，不會更改整個群組的語言。 範例：`/我的語言 印尼文` |
| `/interpreter` | `/口譯` | 群組／私訊 | 開啟雙向語音口譯，輪流說話；連結 10 分鐘有效。 範例：`/口譯` |
| `/whoami` | `/我是誰` | 群組／私訊 | 查看自己的 LINE ID 與管理員身分。 範例：`/我是誰` |
| `/status` | `/狀態` | 群組 | 查看此群組的語言、文字模式及圖片、語音、工單開關。 範例：`/狀態` |
| `/to` | `/私訊語言` | 私訊 | 查看私訊互譯說明；設定閱讀語言請用 /mylang／/我的語言。 範例：`/私訊語言` |

### 作業確認與交班

| 英文 | 中文 | 使用範圍 | 說明與範例 |
|---|---|---|---|
| `/ack` | `/確認` | 群組 | 建立雙語作業確認。按「了解」記錄已理解；「查看回覆」查名單。 範例：`/確認 今天下班前請完成設備檢查。` |
| `/handover` | `/交班摘要` | 群組 | 整理本群組最近 12 小時的翻譯紀錄；摘要需由交接人核對。 範例：`/交班摘要` |
| `/notice` | `/公告` | 群組；管理員 | 產生雙語公告，不建立了解回覆紀錄；需要追蹤請用 /ack／/確認。 範例：`/公告 明天上午八點開會。` |

### 工廠查詢工具

| 英文 | 中文 | 使用範圍 | 說明與範例 |
|---|---|---|---|
| `/factory` | `/工廠` | 群組／私訊 | 開啟設備查詢、雙語作業說明、站別翻譯、複製與分享。 範例：`/工廠` |
| `/qr` | `/掃碼` | 群組／私訊 | 開啟工具後按掃描 QR；相機不可用時可手動輸入設備代碼。 範例：`/掃碼` |
| `/qry` | `/儲區` | 群組／私訊 | 查詢儲區資料；未帶參數會顯示查詢用法。 範例：`/儲區 大成` |
| `/pkg` | `/包裝` | 群組／私訊 | 查詢包裝代碼對應的包裝方式。 範例：`/包裝 1A` |
| `/pw1` | `/班長密碼` | 群組／私訊 | 顯示後台設定的班長密碼資料。 範例：`/班長密碼` |
| `/pw2` | `/儲運密碼` | 群組／私訊 | 顯示後台設定的儲運密碼資料。 範例：`/儲運密碼` |
| `/scrap` | `/廢料` | 群組／私訊 | 查看廢料鋼種的顏色分類。 範例：`/廢料` |
| `/suggest` | `/建議` | 群組 | 取得後台設定的建議表單連結。 範例：`/建議` |
| `/leave` | `/請假` | 群組 | 取得後台設定的請假／出勤入口。 範例：`/請假` |

### 翻譯修正與品質

| 英文 | 中文 | 使用範圍 | 說明與範例 |
|---|---|---|---|
| `/wrong` | `/標錯` | 群組 | 無參數：標記最近一筆；接正確譯文：送出修正；接 N：倒數第 N 筆；list：最近 10 筆。成員修正須管理員核准。 範例：`/標錯 2 正確譯文` |
| `/wrongstats` | `/標記統計` | 群組 | 查看錯誤標記與已核准修正數量，不等同實測翻譯準確率。 範例：`/標記統計` |
| `/export` | `/匯出` | 群組；管理員 | 無參數：已核准訓練資料統計；jsonl：產生有效 1 小時的下載連結。 範例：`/匯出 jsonl` |

### 群組與翻譯開關

| 英文 | 中文 | 使用範圍 | 說明與範例 |
|---|---|---|---|
| `/on` | `/開啟翻譯` | 群組；管理員 | 開啟此群組翻譯；仍遵循個人略過與群組文字模式。 範例：`/開啟翻譯` |
| `/off` | `/關閉翻譯` | 群組；管理員 | 關閉此群組自動翻譯；管理與查詢指令仍可使用。 範例：`/關閉翻譯` |
| `/img` | `/圖片` | 群組；管理員 | on／開啟：自動翻譯；off／關閉：停用；ask／詢問：按按鈕才翻譯。 範例：`/圖片 詢問` |
| `/voice` | `/語音` | 群組；管理員 | on／開啟、off／關閉：控制收到語音訊息時的翻譯。 範例：`/語音 開啟` |
| `/wo` | `/工單` | 群組；管理員 | on／開啟、off／關閉：控制拍工單自動查儲區。 範例：`/工單 開啟` |
| `/panel` | `/設定` | 群組 | 開啟群組語言面板；修改語言須管理員。 範例：`/設定` |
| `/lang` | `/語言` | 群組；管理員 | 無參數：語言面板；接語言代碼：更改群組目標語言，以逗號分隔。 範例：`/語言 id,vi` |
| `/liff` | `/群組設定` | 群組；管理員 | 開啟手機設定頁，調整語言、翻譯、朗讀與保留詞。 範例：`/群組設定` |

### 人名與保留詞

| 英文 | 中文 | 使用範圍 | 說明與範例 |
|---|---|---|---|
| `/skip` | `/略過我` | 群組 | 此群組略過你的訊息，不再自動翻譯。 範例：`/略過我` |
| `/unskip` | `/恢復我` | 群組 | 恢復自動翻譯你在此群組的訊息。 範例：`/恢復我` |
| `/skiplist` | `/略過名單` | 群組 | 查看此群組不翻譯的成員名單。 範例：`/略過名單` |
| `/skipadd` | `/略過成員` | 群組；管理員 | 依已辨識姓名加入不翻譯名單；重名時需更完整姓名。 範例：`/略過成員 姓名` |
| `/skipdel` | `/恢復成員` | 群組；管理員 | 從此群組不翻譯名單移除指定成員。 範例：`/恢復成員 姓名` |
| `/skipterm` | `/保留詞` | 群組；管理員 | 無參數：清單；接詞：新增；-詞 或 del 詞：刪除；clear／清空：清除此群組自訂詞。 範例：`/保留詞 -產品名稱` |

### 管理與診斷

| 英文 | 中文 | 使用範圍 | 說明與範例 |
|---|---|---|---|
| `/clearcache` | `/清除快取` | 群組；管理員 | 清除記憶體翻譯快取；不刪除詞庫、修正資料或紀錄。 範例：`/清除快取` |
| `/audit` | `/抽檢` | 私訊；管理員 | 私訊抽取最近 N 筆翻譯供人工核對，預設 10、最多 20。 範例：`/抽檢 10` |
| `/mark` | `/評分` | 私訊；管理員 | 先用 /audit／/抽檢，再標記樣本編號：o／對 或 x／錯。 範例：`/評分 1 對` |
| `/auditstats` | `/抽檢統計` | 私訊；管理員 | 查看人工抽檢累計結果與正確比例。 範例：`/抽檢統計` |
| `/ttscheck` | `/朗讀檢查` | 私訊；管理員 | 檢查朗讀設定；條件齊全時會合成測試語音，可能產生 API 費用。 範例：`/朗讀檢查` |
| `/setprice` | `/設定收費` | 私訊；管理員 | 私訊查看或更改服務收費說明，可輸入多行文字。 範例：`/設定收費 收費說明` |

## 檔案清單

- `.github/workflows/factory-translation-release-gate.yml`
- `app.py`
- `factory_pmi_semantics.py`
- `line_command_catalog.py`
- `line_factory_features.py`
- `line_message_ui.py`
- `line_quick_reply.py`
- `static/admin_factory.js`
- `static/interface_theme.css`
- `static/liff_forms.js`
- `static/line_factory.css`
- `static/line_factory.js`
- `templates/line_factory.html`
- `test_factory_receipt_flow.py`
- `test_ui104_commands.py`
- `tests/member_ui_lifecycle.cjs`
- `tests/run_factory_ui.py`
