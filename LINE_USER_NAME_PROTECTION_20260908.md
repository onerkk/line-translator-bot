# LINE 使用者名稱自動保護

本次以 GitHub `onerkk/line-translator-bot` 的 `4ed73276d98e712f26ff97365552485bf7a1b744`（#81）為基準，只提供本次變更檔案。

## 更新後如何使用

1. 解壓縮 ZIP，將裡面的檔案一起上傳至 GitHub 專案根目錄，覆蓋同名檔案。`line_user_names.py` 是新增的必要模組，請和 `app.py` 一起上傳。
2. 等待 GitHub 檢查及 Render 部署完成。
3. 打開管理後台 →「保護名單」→「重新整理名單」。既有使用者會自動補入，並顯示「👤LINE 自動」。
4. 新使用者傳訊息、加入群組／多人聊天室，或加入機器人好友，取得 LINE 個人資料後，就會自動保護完整顯示名稱。

不需要 AI 金鑰的新設定，也不需要手動逐一新增姓名。

## 實際行為

- 直接沿用既有的群組／私訊使用者紀錄。已存過的使用者會立即出現在保護名單，重啟後也會依持久設定重新建立。
- 保留完整顯示名稱、空格、中英混合、括號及表情符號；不從聊天句子猜測人名，也不自動拆成不確定的暱稱。若需要另一個簡稱，可手動新增。
- 與手動名單、儲區客戶合併去重。同名可以同時標示「LINE 自動」和「儲區」；不同人的相同名稱只顯示一次，但仍以使用者 ID 分別記錄。
- 名稱變更後，更新該使用者在所有已知聊天室／私訊的紀錄。舊名若仍是手動別名，或仍有另一位使用者使用，就繼續保留。
- LINE 自動名稱由個人資料管理，儲區名稱由儲區資料管理；手動名稱仍可在後台新增、移除。
- 只在原文出現的名稱才送進翻譯保護條件。`Adi` 不會誤套到 `tadi`，`Ann` 不會誤套到 `announcement`；名稱也不會被當成 LINE 使用者 ID。
- 名稱加入或更新時，翻譯快取會使用新的保護條件；舊快取若改寫或漏掉來源中的姓名，會被捨棄。

## 速度與同步時機

使用現有 LINE 個人資料快取，成功查詢的快取期為 10 分鐘。已知使用者改名以後，會在快取到期後的互動中啟動背景更新；不保證在 LINE 改名的瞬間就同步。

姓名與頭像共用一次查詢。無頭像不會重複查詢；背景更新最多同時處理 4 人，超過時由後續互動重試。新增姓名保護本身不增加 AI 呼叫。第一次取得未知使用者資料仍需要 LINE API 查詢；若 LINE 暫時無法提供資料，保留既有姓名，後續互動重試，繼續處理原訊息。

## LINE 官方能力範圍

Webhook 提供使用者 ID，機器人再透過對應的個人資料 API 取得顯示名稱。群組、多人聊天室及私訊使用各自的 API。

一般未認證官方帳號不能保證直接取得所有尚未互動成員的完整名單。這次會自動補入機器人已知的使用者，並隨新互動／加入事件擴充；不宣稱能繞過 LINE 的成員名單權限。

- [取得使用者 ID 與完整成員名單的限制](https://developers.line.biz/en/docs/messaging-api/getting-user-ids/)
- [Webhook 與個人資料](https://developers.line.biz/en/docs/messaging-api/receiving-messages/)
- [群組成員個人資料 API](https://developers.line.biz/en/reference/messaging-api/#get-group-member-profile)

## 變更檔案

- `app.py`：整合目錄、背景更新、保護邊界、快取與後台。
- `line_user_names.py`：名稱去重與完整名稱邊界辨識。
- `test_line_user_name_protection.py`：自動加入、改名、同名、持久化、權限與翻譯保護回歸測試。
- `test_storage_protected_name_sync_root_fix.py`：隔離儲區測試的使用者資料，保留原本斷言。
- `LINE_USER_NAME_PROTECTION_20260908.md`：本說明。

本次提供可上傳的程式檔，尚未替你部署至 Render，也未向真實 LINE 群組發送測試訊息。

## 驗證結果

- Python 3.12 全套回歸：1,646 項測試、438 項子測試全部通過（49.74 秒）。
- 翻譯資源驗證：通過，無錯誤、無警告。
- 既有後台／LIFF／表單／統一選單互動測試：通過，使用本機假 LINE／AI 傳輸。
- 實際保護名單 JavaScript：語法與 DOM 測試通過，涵蓋來源標記、計數、同名來源、手動刪除、空名單及 HTML 字元跳脫。

以上為離線回歸與本機互動驗證，不等於已完成線上部署或實際 LINE API 端到端測試。
