# 2026-09-11 翻譯效能與提示一致性更新

本包接續先前的自動學習、停止追蹤卡片、自訂提醒卡片三包更新。只包含本輪變更，保留相對路徑覆蓋原專案後部署；`app.py`、`prompt_optimizer.py`、`factory_semantic_audit.py`、`translation_request_cache.py` 四個執行檔須一起更新。不需要新增套件、API 金鑰或教學範例。

## 是否已到極限

還不能宣稱已到絕對極限。速度、費用、品質涉及取捨，也受實際訊息、快取命中率、模型及網路影響。本輪只採用找到根因、可以驗證的改善；未取得正式服務的延遲分布、帳單或雙語人工評分，不能聲稱正式 LINE 一定快多少、帳單少多少，或翻譯百分之百正確。

## 本輪修正

1. **語意判斷有明確優先權。** 以當前原文建立的語意框架、已解析的歧義和硬性術語，提供詞義所屬的來源標記。當這些資料已定義某詞，提示編譯器不再附上同一來源詞的舊字典定義。例如「入非本月」已解析為系統紀錄分類時，不再同時送出 `非本月=bukan order bulan ini`。不採用舊案例中偶然出現的詞作為刪除依據；相似字串或不同來源詞也不會被一併刪掉。
2. **硬性術語只列一次。** 當來源術語表已含完全相同的 HARD 對應，明確要求使用精確目標詞，不再追加第二張相同表格。SOFT 語意指引、不同對應及尚未列出的術語仍保留。
3. **長公告共用定義、分別保留每項指示。** 同一意思與目標概念重複出現時，定義列一次；每個 claim ID、原文依據及所屬項次仍各自存在。不同否定、動作狀態或相反指令不能合併。原文、數字、語意條件及驗證資料不被截斷。
4. **整理本機資料複製與寫入路徑。** 純分析資料使用專門的容器複製路徑；保留共享子物件、循環參照及自訂型別的 deepcopy 行為。每次快取讀取仍拿到獨立可修改的結果。診斷 JSON 改為一次序列化、一次寫入並原子替換，保留全部欄位，減少長公告產生的零碎檔案寫入。
5. **持續驗證不應產生正式 API 流量。** CI 改用專案已有的離線測試執行器，先封鎖外部 HTTP、DNS 與 socket，再載入應用程式和測試；只允許 SDK 連線重用測試需要的本機 loopback。後台 DOM 測試仍使用假 LINE／AI 傳輸。

原有自動學習仍會從通過驗證的修正提煉規則，並用於第一次翻譯請求。此次編譯仍保留學習規則、來源語意、群組／語言方向隔離與撤回機制；沒有把模型權重宣稱成自動訓練，也沒有新增整句範例捷徑。

## 實測

相同 15 組來源、三家實際 SDK 適配器送出格式的比較（回應為固定測試資料）：

| 提供者 | 修改前提示字元總數 | 修改後 | 減少 |
| --- | ---: | ---: | ---: |
| Claude | 66,029 | 62,609 | 5.18% |
| OpenAI | 66,029 | 62,609 | 5.18% |
| Gemini | 63,494 | 60,074 | 5.39% |

以上是送出文字的字元數，**不是計費 token、實際帳單或節省金額**。兩版本使用相同模型等級，每個 SDK 案例均只有一次模擬請求。重複且語境完全一致的六次翻譯，新舊版本均為 `[1, 0, 0, 0, 0, 0]` 次生成；這是保留既有能力，不列為本輪新增的節省。

本機速度：前／後／後／前／前／後循序執行，每輪各案例七次取中位，再取同版本三輪的中位數。含真實本機 LINE 訊息處理、校驗與卡片組裝；AI／LINE 傳輸為模擬，背景後處理不在量測範圍。

| 案例（未命中快取） | 修改前本機毫秒 | 修改後本機毫秒 |
| --- | ---: | ---: |
| 一般短句 | 53.035 | 60.886 |
| 套環補裝 | 54.595 | 57.202 |
| 長公告 | 84.798 | 83.640 |
| 結構化紀錄欄位 | 30.431 | 35.719 |
| 入非本月／換 TAG 問句 | 52.921 | 62.523 |

**此批速度數據有多項變慢，不能宣稱整體加速。** 後續分離呼叫執行緒 CPU 計時，仍觀察到情境間與批次間波動；完整數值一併保留。本輪可以確認提示變短及重複資料操作減少，尚不足以量化正式 LINE 速度改善，更不能稱為速度極限。

為再確認批次負載的影響，另讓新舊兩個版本常駐，對每個案例相鄰交替執行 15 組比較，先排除兩輪暖機。差異仍小且方向不一致：

| 案例 | 修改前本機毫秒 | 修改後本機毫秒 | 配對差值中位數（後減前） |
| --- | ---: | ---: | ---: | ---: |
| 一般短句 | 50.066 | 51.013 | +1.543 |
| 套環補裝 | 47.414 | 46.926 | -0.458 |
| 長公告 | 89.942 | 92.156 | +1.661 |
| 結構化紀錄欄位 | 25.999 | 26.281 | +1.119 |
| 入非本月／換 TAG 問句 | 52.701 | 52.049 | +0.008 |

正差值代表這次量測較慢，負差值代表較快。這些本機毫秒數不含真正的模型生成或 LINE 傳輸，不能代替正式回覆延遲。所有配對數據與 CPU 計時均附在 JSON 報告。

最終完整離線回歸：**2,836 項測試、443 個子測試通過**，含新增 14 項；執行 78.00 秒是測試耗時，不是翻譯延遲。DOM 操作與版本化素材檢查通過，素材錯誤／警告均為零。

`TRANSLATION_EFFICIENCY_MEASUREMENTS_20260911.json` 附完整新舊量測、案例雜湊、執行檔版本雜湊、模型及請求數。速度基準新增 caller CPU 時間，明確區分牆鐘時間與執行緒運算時間。

## 品質與限制

新增回歸包含：實際 provider 邊界不再同時送出衝突詞義、硬性術語精確且不重複、長公告每項來源事實完整、相反指令不能互相借用、縮小可選提示預算仍保留學習和強制條件、快取結果修改不會污染後續檢查，以及診斷資料完整落盤。

本輪沒有降低模型等級、關閉語意／數字／否定／順序檢查，或增加日常翻譯呼叫。既有有界修復、備援切換及快取重查仍運作。已驗證的相同來源／相同語境可零次呼叫重用；未知語意及高風險訊息仍依原有政策使用模型或複核，不能靠本機關鍵字規則保證所有語言現象正確。

提示編譯器與語意框架版本已一起更新；既有版本化快取會依新版本重新判斷可用性，無須手動刪除資料庫。原有三款卡片與提醒排程沿用前包。

## 重跑方式

使用專案原有 runtime／test dependencies：

```sh
python run_translation_offline_checks.py
python validate_factory_translation_assets.py --json
python benchmark_translation_speed_economy.py --repo /path/to/before --output economy-before.json
python benchmark_translation_speed_economy.py --output economy-after.json
python benchmark_translation_latency.py --repo /path/to/before --output latency-before
python benchmark_translation_latency.py --output latency-after
python benchmark_translation_paired.py --before /path/to/before --output paired.json
```

基準程式會封鎖外連並建立獨立資料庫；API 與 LINE 回應使用固定測試資料。速度比較應循序交錯執行兩版本，避免同時跑其他測試干擾。前端 DOM 測試使用既有 `tests/run_factory_ui.py` 與 `JSDOM_PATH`，步驟已在 CI 設定。

## 判斷依據

減少冗餘輸入有助降低輸入用量，但輸出生成往往才是主要模型延遲來源，不能把提示縮短比例當成回覆加速比例。[OpenAI 延遲最佳化](https://developers.openai.com/api/docs/guides/latency-optimization)

快取需要相同前綴和符合模型門檻；不是開啟選項便一定命中。本輪保留固定原則在前、動態來源在後，沒有為湊門檻而填充提示。[OpenAI Prompt Caching](https://developers.openai.com/api/docs/guides/prompt-caching)、[Claude Prompt Caching](https://platform.claude.com/docs/en/build-with-claude/prompt-caching)、[Gemini Context Caching](https://ai.google.dev/gemini-api/docs/caching)

本機成本儀表板可用來觀察實際 usage，但正式費用仍須核對供應商帳單；本報告不以模擬 token、字元或既有歷史註解推算節省金額。本次未部署正式服務。
