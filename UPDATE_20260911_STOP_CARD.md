# 作業確認：提前停止追蹤通知卡

版本：2026-09-11.2-early-stop-card

## 套用

本 ZIP 接續上一包 `line-translator-smart-learning-20260911-changes.zip`，只包含這次新增或修改的檔案。先保留前一包的翻譯更新，再將本包檔案覆蓋到專案根目錄並重新啟動服務。沒有新增套件、金鑰或人工資料搬移步驟；SQLite 需要的新索引會自動建立。

## 卡片與行為

- 發起人或管理員首次提前停止作業確認追蹤後，在原群組發出一張小型中印雙語 Flex 卡片；群組停止按鈕與後台停止功能都適用。
- 深藍底、霧金點綴，使用 `kilo` 寬度，沒有圖片、大型進度區或操作按鈕。本地排版預覽約 250 × 235；實際外觀由 LINE 裝置與字型設定決定。
- 主標題：**已停止作業追蹤**／**Pemantauan dihentikan**。
- 告知內容：**本次追蹤提前結束，不再提醒。**／**Pemantauan konfirmasi diakhiri lebih awal. Tidak ada pengingat lanjutan.**
- 保留「確認紀錄保留 · Catatan tetap tersimpan」、通知短編號與 UTC+8 停止時間，方便對照是哪一筆通知。紀錄沿用既有保存期限，不因重複點擊延長。
- 停止追蹤不等於實際作業完成；原有確認紀錄與後續有效的「了解」回覆仍可保留。全員確認完成時沿用原本完成卡，不另外補發提前停止卡。

## 發送可靠性與消耗

停止狀態與待發送卡片在同一筆原子更新中保存，成功後才發送。重複點擊、Webhook 重送與兩人同時操作共用相同卡片及 LINE Retry Key，保留第一次停止的時間與操作者。

超時或可重試錯誤由現有排程工作程序自動補送，不需要再次點擊。補送僅處理停止告知，不重開提醒。發送前重新核對原文是否仍有效、通知期限、群組開關及機器人成員狀態；歷史停止紀錄不會補發新卡。若原卡已顯示但初次送達狀態未成功保存，有效的停止按鈕事件可證明送達並正常告知。

卡片直接由程式生成：零翻譯／模型 API、零圖片載入、零成員名單或個人資料查詢。正常情況每筆提前停止增加一次 LINE Push 請求，會使用既有 LINE 訊息額度。重試採退避並限制在 23 小時內，不跨越 LINE 的 24 小時 Retry Key 有效期。LINE 接受請求後不再補送；接受請求不等於保證每位成員已看到。

自動補送沿用既有 `FACTORY_ACK_WORKER_ENABLED`／`REMINDERS_WORKER_ENABLED` 設定。工作程序停用時，首次仍立即嘗試發送；待辦會保存到恢復工作程序或再次執行有效停止操作時處理。

LINE 官方行為依據：[重試 API 請求](https://developers.line.biz/en/docs/messaging-api/retrying-api-request/)。

## 檔案

| 檔案 | 更新內容 |
|---|---|
| `line_message_ui.py` | 新增小型雙語停止告知卡 |
| `line_ack_stopped.py` | 停止告知的原子待辦、發送租約、固定 Retry Key、重試及失效檢查 |
| `line_factory_features.py` | 群組及後台停止操作接入告知，保留首次停止資料 |
| `line_ack_reminders.py` | 現有工作程序處理停止告知補送 |
| `line_factory_store.py` | SQLite／Redis 原子到期索引涵蓋停止告知 |
| `test_ack_stopped.py` | 停止告知及異常恢復回歸測試 |
| `test_ack_repeat_controls.py` | 更新停止操作的通知預期 |
| `test_line_factory_features.py` | 共用離線 LINE 發送替身 |
| `UPDATE_20260911_STOP_CARD.md` | 本次說明 |

## 驗證

SQLite 與 Redis 均使用實際儲存／原子更新流程測試；只有外部 LINE 與 AI 傳輸使用離線替身。已核對 SDK Flex 訊息格式及本地排版預覽。測試涵蓋群組與後台停止、未授權操作、重複點擊、同時操作、寫入失敗、超時、送達後狀態寫入中斷、重新啟動、原文失效、關閉群組、機器人離群及 LINE 200／409／400／429／500 回應。

交付前完整回歸：**2,803 項測試、443 個子測試全部通過**，其中新增停止通知測試 57 項。工廠前後台 DOM 操作測試、既有前端語法檢查與翻譯資產驗證也通過；資產驗證沒有錯誤或警告。

未部署到正式環境，也未向正式 LINE 群組發送測試訊息。
