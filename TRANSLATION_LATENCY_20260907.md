# 2026-09-07 翻譯等待時間優化

## 安裝

基準版本：`ca8707af4139f1555eb7edad100939fffc1a3f01`。

本包只有 15 個修改或新增檔案，全部放在專案根目錄。請一次上傳並覆蓋同名檔案，尤其要包含新增的 `translation_request_cache.py`；其他原有檔案請保留。等待 GitHub「Factory translation release gate」通過、Render 顯示 Deploy succeeded 後才會生效。沒有新增環境變數或必須調整的後台設定。

## 改動

1. 一則翻譯內，相同原文的語義、數量、不可改動資料分析可共用。完成或失敗時清除，同工作執行緒下一則翻譯建立全新範圍，並行工作不共用可變快取。
2. 完整原文、譯文、語言方向、當次詞庫與段落規則完全相同時，共用已完成的本地驗證報告。改字、還原人名、表情修飾或更新詞庫都會重新驗證。呼叫者收到獨立的巢狀資料，修改回傳值不會污染後續檢查。
3. 知識庫讀取仍確認目前檔案版本；同版本、同查詢只計分一次。案例快照在載入時準備，讀取時快速解碼，避免反覆深複製整批案例。
4. SQLite 用持久化的結構版本識別已完成遷移的資料庫。正常讀取不用反覆取得遷移寫鎖；真正的佇列、發送檢查點、租約與回執仍正常寫入，WAL 與 synchronous=FULL 保留。舊資料庫與替換後的新資料庫仍可遷移。

沒有更換模型、提高費用方案、刪除否定／時間／數量／工廠意思檢查，或以縮短逾時造成更早失敗來充當加速。原有來源完全相同且合格的翻譯快取與安全降級策略持續使用。

## 同一測試程式的前後對照

三輪獨立執行，每情境共 21 次正常計時，取中位數。暖機與 cProfile 計時不計入中位數。範圍為 `handle_message` 的本機執行：原文分析、提示組裝、驗證、持久佇列、LINE 訊息組裝；AI 回應與 LINE 傳輸使用固定測試替身，背景訓練／統計工作排除。網路封鎖，付費 AI 呼叫與真實 LINE 發送均為 0。

| 情境 | 狀態 | 優化前 | 優化後 | 變化 |
|---|---|---:|---:|---:|
| 一般工作短句 | 未命中快取 | 94.9 ms | 73.9 ms | 減少 22.1% |
| 一般工作短句 | 命中快取 | 69.4 ms | 51.1 ms | 減少 26.4% |
| 套環要補上 | 未命中快取 | 90.3 ms | 71.6 ms | 減少 20.8% |
| 套環要補上 | 命中快取 | 51.5 ms | 52.7 ms | 增加 2.4% |
| 五點工廠長公告 | 未命中快取 | 160.9 ms | 99.5 ms | 減少 38.2% |
| 五點工廠長公告 | 命中快取 | 97.0 ms | 68.1 ms | 減少 29.8% |

「套環要補上」已命中快取的情境未量得加速，增加約 1.2 ms；完整結果一併保留，不只列出加速案例。其餘測試情境的本機中位數降低約 21%～38%。這些數字不是所有訊息的平均值，也不含 Render 喚醒、AI 推論或網路延遲，不能視為線上整體速度保證。

六個情境的模型、提示字元數、提示 SHA-256 與請求次數逐一相同；未命中快取為 1 次模擬 AI 請求，命中為 0 次。長公告 cProfile 中，訊息語義分析從 26 次降為 1 次、數量分析從 25 次降為 1 次。9 個品質驗證入口仍被呼叫，只有 2 組不同完整輸入需要實際重算；24 次重複資料庫欄位／遷移檢查降為 0 次。

完整原始計時、固定輸出、模型與提示指紋見 `TRANSLATION_LATENCY_BENCHMARK_20260907.json`。這不是 AI 翻譯準確率或實際帳單測試。

## 驗證

- 完整發布測試：1,452 項測試、438 項子測試通過，0 失敗。
- 新增 15 項測試：請求／執行緒隔離、回傳值及輸入變更、失敗後重試、容量上限、相反操作狀態、保護名稱、詞庫與段落規則變更、知識更新、SQLite 真正寫入權限與舊工作遷移。
- 工廠素材檢查：30 個正式案例通過，29 個錯譯探針被拒絕，無錯誤、無警告。
- 前一版的套環、否定、疑問、時間、動作與主詞修正測試維持通過。

重現命令（安裝專案測試依賴後）：

```bash
python benchmark_translation_latency.py --output /tmp/latency-after
python benchmark_translation_latency.py --repo /path/to/previous-version --output /tmp/latency-before
python -m pytest -q test_*.py
python validate_factory_translation_assets.py --json
```

## 部署後確認實際速度

在測試群組先發一般短句，再發相同短句一次，最後發一則完整公告。在 Render Logs 對照同一時間附近的 `[Perf]` 與 `[DeliveryPerf]`：`api` 是模型呼叫耗時，`sent` 是該請求從開始處理到發送成功的時間。若 `api` 或服務喚醒佔主要時間，本次本機優化不能消除那部分等待。尚未取得這次版本的正式環境前後測速，因此不宣稱已達物理極限或保證每句固定秒數。
