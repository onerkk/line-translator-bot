# 翻譯中斷：暫時限額誤鎖修正

本次修正的是已經在離線完整流程重現的供應商誤鎖漏洞。沒有正式站台日誌，尚不能證明截圖當時就是這個原因，也沒有部署或連線確認正式機器人已恢復。

## 查到的問題

`ai_provider.py` 原本把 `exceeded your current quota` 當作所有供應商共用的餘額耗盡證據。Gemini 的暫時限額錯誤也可能包含這句文字，於是被寫入 `quota_exhausted_providers`，後續請求、補送及重新啟動都會跳過它。即使限額已重置，單一 Gemini 供應商的部署仍可能一直沒有翻譯。

這是供應商資格被錯誤停用的問題，與翻譯句子是否夠短、是否存在特定範例無關。

## 本次變更

- 優先讀取 SDK 的結構化錯誤碼，區分暫時速率／每日用量限制與真正餘額耗盡。
- OpenAI 的舊版純文字帳務判斷只適用 OpenAI；不再套到 Gemini。
- 寫入永久停用狀態前再次驗證帳務錯誤，避免其他呼叫路徑誤寫。
- 每個 worker 載入設定時排除有明確暫時限額證據的舊誤鎖。真正欠費、無法確定原因的舊紀錄、使用者選定的主力供應商仍保留。這個載入遷移不額外寫檔；正常儲存設定時會保存清理後的資料。
- 新帳務停用紀錄保留判定類型，即使錯誤文字被截短或使用其他語言，也不會被當成舊誤鎖清除。
- 後台健康檢查 JSON 的 `translation_runtime.providers.error_policy_build` 顯示 `2026-09-11.4-provider-specific-quota-recovery`，方便確認程式版本。

沒有新增整句固定翻譯、放寬品質檢查或增加正常翻譯的 API 呼叫。沿用既有請求次數、時間與生成上限，以及持久補送佇列。

## 重現與驗證

以原始兩句 `不要套環`、`@阿堂 短尺收 照捆包包` 測試真正的供應商協調器、翻譯管線、品質檢查、標註還原與 LINE 回覆組裝。API 與 LINE 傳輸使用固定模擬回覆，並封鎖對外網路。

覆蓋無照片、照片待辨識、已辨識工單，以及重複 webhook。正常情況兩句皆只生成一次、傳送一次；標註與照片本身未造成重現案例漏翻。

修正前首批 28 項新增測試有 10 項失敗，包括「限額錯誤後 API 恢復仍無法補送原句」與「重啟仍被誤鎖」。修正後增加結構化帳務碼、設定重載與永久停用邊界檢查；35 項新增測試通過，與相關既有測試合計 85 項通過。完整測試：2,871 項及 443 項子測試通過（74.59 秒，為離線測試套件執行時間，不是翻譯延遲）。詳見同包驗證紀錄。

這些結果證明程式在所測情境的行為；不代表已量測正式模型準確率、實際帳單、正式 API 延遲或確認截圖事件的原因。

## 套用

將 ZIP 內檔案依原路徑覆蓋到先前更新完成的同一專案，重新部署／啟動所有 worker。不要刪除既有設定檔、資料庫或待補送佇列。本次執行期變更只有 `ai_provider.py`；新增測試由既有離線檢查工具自動收錄。

部署後可在既有後台「健康檢查」確認上述版本、可用供應商及佇列狀態。已被妥善保存在佇列的訊息，會在供應商恢復後繼續補送；這次更新無法補回從未收到或已遺失的訊息。

若正式環境仍未回覆，需要提供出事時段的健康檢查與事件紀錄，才能進一步區分：未收到 webhook、群組設定略過、無可用供應商、翻譯品質拒收或 LINE 傳送失敗。無須提供金鑰。

離線驗證指令：`python run_translation_offline_checks.py`。

## 判斷依據

- [Google：限額的計算與重置](https://ai.google.dev/gemini-api/docs/rate-limits)：每分鐘請求、每分鐘 token 與每日請求上限屬不同限制，不能一律判成餘額歸零。
- [Google：暫時錯誤的重試](https://ai.google.dev/gemini-api/docs/troubleshooting)：對可重試的 429／503 使用有界退避。
- [OpenAI：錯誤碼](https://developers.openai.com/api/docs/guides/error-codes)：速率限制與帳務額度不足需要分別處理。
