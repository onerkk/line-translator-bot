# 翻譯與學習系統更新

將本 ZIP 內的檔案覆蓋專案中的相同路徑，再重新部署 Render。所有執行程式檔案須一起更新，啟動時會檢查版本是否一致。

## 本次行為

- 保留自動學習、人工修正、翻譯記憶及歷史學習資料。驗證通過的本機修正也能形成學習規則，不需要再呼叫 AI 審查。
- 已知錯誤類型會進入後續翻譯的第一次提示；有疑慮的譯文仍可送出，但不當成正確答案寫入翻譯記憶。
- 品質規則只做本機修正與診斷，不再拒收已有譯文。月底期間、入庫數量及入帳時間分開判讀，避免這次公告遭到錯誤攔截。
- 每個翻譯請求最多一次 AI 生成；停用品質重翻、二次審查、SDK 自動重試與參數修正後重送。舊環境設定不能重新開啟這些行為。
- 僅在沒有取得譯文時，可使用本機備援及一條選定的 NMT 備援；同一請求不重複呼叫已嘗試的 NMT，也不因品質警告再換路線。
- 已嘗試的背景工作失敗後保留失敗紀錄，不重新排隊。舊版已重試或已取得租約的待辦也會終止，避免部署後接著消耗 API。
- LINE 重複投遞不重新翻譯。同批事件其中一則失敗時，其餘事件仍各處理一次；部分通知語言已有譯文時，保留並送出可用語言。
- 使用者原本設定的公告定時提醒仍保留；提醒使用已完成的通知內容，不重新翻譯。

若上游服務完全沒有回傳內容或 LINE 未接受傳送，工作會留下失敗紀錄，無法憑空產生成功譯文，也不會以自動重試持續扣費。

## 檔案與驗證

本包僅含新增與變更檔案，不含金鑰、環境設定、學習資料庫或其他執行期資料。保留原本的資料目錄即可沿用既有學習紀錄。

本版累計包含單次生成修正、十張截圖語意修正，以及使用者確認的入庫／TAG／木箱／三個包裝站知識。最新驗證結果見 `SCREENSHOT_TRANSLATION_REVIEW.md`。測試使用模擬 AI 與 LINE 傳輸，未呼叫正式服務；本包尚未替您部署到 Render。

專案內可用以下指令重跑離線檢查：

```sh
python validate_factory_translation_assets.py --json
python run_translation_offline_checks.py
```
