已針對正式版本 `3ff0c9de90a9bb6386c671f7f9f94cbab449124a` 完成翻譯延遲優化。本包只含本次變更的程式、測試與驗證說明。

本次查到並在 GPT-5.6 路徑離線重現的程式問題：工廠模式每次送出的提示詞包含 4,856 字元固定規則，但舊版快取邊界只涵蓋前面的原則區段。舊程式估算該區段為 824 tokens，未達它使用的 1,024 門檻，因此即使設定 explicit 模式，也沒有加入快取斷點。正式日誌當時未記錄模型與快取 tokens，不能斷言先前所有 AI 等待都由這個問題造成。

現在將完整固定規則放入可重用前綴，原文、客製語氣、術語、語意條件與上下文仍各自隨請求送出。相同前綴與翻譯方向使用相同的雜湊路由鍵，提高不同群組重用固定規則的機會。這不會共用群組譯文或對話內容。新增的快取標記也不會擠掉原先提示詞預算內的相關術語。

以兩句實際問題句跑完整離線文字流程，並用同一份輸入比較新舊編譯器：

| 項目 | 修改前 | 修改後 |
| --- | ---: | ---: |
| 每句編譯後提示詞 | 7,186 字元 | 7,227 字元 |
| 固定前綴 | 1,649 字元 | 6,553 字元 |
| 工廠模式的明確快取斷點 | 沒有 | 有 |
| 正常每句生成請求 | 1 次 | 1 次 |

字元數與程式內 token 估算不是帳單 token。SDK 的模擬 HTTP 測試已驗證實際送出的 JSON 有正確快取斷點；模擬回覆中的 cache tokens 只用來驗證日誌解析，不能當成正式環境命中率。

四組原有語氣規則全文相同。模型選擇、品質驗證、數字／人名／否定／作業順序檢查、API 重試預算、持久化送達及去重機制均沿用原有流程；沒有增加預熱 API、競速 API 或新的套件。

**部署方式**

請將以下五個程式檔一起覆蓋專案根目錄，放在同一次提交中部署：`app.py`、`ai_provider.py`、`prompt_optimizer.py`、`webhook_runtime.py`、`line_factory_features.py`。不要只覆蓋 `app.py`；啟動檢查會核對提示詞編譯器版本。新版編譯器為 `2026-09-11.4-reusable-factory-prefix`。測試與說明檔也可一併提交。

沿用原有設定即可。GPT-5.6 系列使用此 explicit 快取配置；既有 `OPENAI_TRANSLATION_EXPLICIT_CACHE=0` 和呼叫端自訂快取政策仍受尊重。不同模型、被停用的快取或不支援此參數的端點，不保證得到同樣效果。

**部署後如何確認加速**

在 [Render 服務頁](https://dashboard.render.com/web/srv-d6jbinftskes738ip6t0) 的 Logs 搜尋下列標記，以相同 `trace` 對照同一則訊息：

| 標記 | 用途 |
| --- | --- |
| `[TranslationStart]` | 文字／媒體處理開始，附收件與排隊時間 |
| `[AIRequest]` | 實際供應商、模型、每次 SDK 耗時、成功／錯誤類型，以及供應商回傳的輸入、輸出、快取讀寫 tokens |
| `[TranslationStages]` | 翻譯流程、使用者資料查詢、送達與 LINE reply／push 的分段耗時 |
| `[DeliveryPerf]` | 原有事件到 LINE 接受回覆的整體耗時 |

`cache_breakpoint=true` 表示請求有設定斷點；`cached_input_tokens>0` 才是供應商回報的實際快取讀取。欄位為 `null` 表示供應商沒有回報，不能算成命中或零費用。分段時間為包含子步驟的時間，不能將 `translation` 與它包含的 AI 時間，或 `delivery` 與它包含的 `line_reply` 再加總。

先看 `sdk_ms` 是否降低，再看 `event_sent_ms`；後者截至 LINE 接受回覆，不是手機實際顯示時間。請比較相同模型、類似長度的訊息，並區分第一筆冷快取和後續命中請求。首次寫入、快取失效、供應商負載、LINE 網路與主機冷啟動仍會影響時間，不能保證每句低於 5 秒。

依 [OpenAI 快取文件](https://developers.openai.com/api/docs/guides/prompt-caching)，此協定的首次寫入與後續讀取計價不同，因此一次性／冷快取請求不一定更便宜。這次只快取可重用固定規則，不拿每句變動內容湊門檻。依 [OpenAI 延遲文件](https://developers.openai.com/api/docs/guides/latency-optimization)，快取與輸入縮減的效果也不能直接按字元比例換算成總時間。

**驗證範圍**

精確測試結果與檔案 SHA-256 見 `LATENCY_OPTIMIZATION_VALIDATION_20260911.json`。離線基準測試會列出修改前後的本機耗時及生成次數；它替換了 AI 與 LINE 傳輸，無法量出雲端快取帶來的加速。本機耗時有數毫秒波動，不能拿來聲稱正式翻譯已降到某個秒數。

此包完成的是程式優化與離線驗證，尚未部署到您的 Render，也未呼叫付費 API 或傳送真實 LINE 訊息。先前某則訊息未翻譯的確切原因，仍不能只由當時缺少 callback 紀錄的日誌判定；本次新增記錄能協助後續定位，但不能倒推出過去事件。
