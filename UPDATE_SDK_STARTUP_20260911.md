本次依正式日誌 `trace=5c9b10c2401a` 修正首次 SDK 載入等待，基準提交為 `bbe95eaf4eeabc28d0f053ce734a39f4c4cef98c`。已核對該提交的八個上傳檔案與本機基準內容完全相同。

**這筆日誌已知的事實**

「要加套環」已產生譯文並送達 LINE，callback 回傳 200，但事件到 LINE 接受回覆為 10.061 秒。

| 不重複計算的時間區間 | 秒數 |
| --- | ---: |
| 實際 SDK 呼叫（含 API 往返及 SDK 回應處理） | 2.470 |
| 供應商流程中、SDK 呼叫外的時間 | 4.506 |
| 其餘前後處理、入站及 LINE 送達 | 3.085 |
| 合計 | 10.061 |

4.506 秒來自 `ai_ms=6976` 減去 `sdk_ms=2470`；3.085 秒來自 `event_sent_ms=10061` 減去 `ai_ms=6976`。`line_reply` 的 0.197 秒包含在第三列，不能再加一次。`translation`、`delivery` 與 `Perf api` 也都是較大的包覆區間，不能當作各自獨立步驟相加。

供應商回報 `cache_write_tokens=3163`、`cached_input_tokens=0`：這筆有快取寫入，但沒有讀取既有快取。它不能作為快取命中後速度的測量。模型為 `gpt-5.6-luna`，回報 reasoning tokens 為 0，只有一次實際傳輸嘗試。

**查到並處理的程式問題**

原流程會在第一筆翻譯執行到 `client.chat.completions.create` 時，才載入 SDK 的資源模組與大量資料型別。Python 會先求出這個方法，才進入 `_sdk_create()`，因此這段載入時間不在原先的 `sdk_ms` 內。

現在在設定載入完成、服務接受訊息之前，先載入已設定且可用供應商的 SDK 資源。OpenAI 與 Gemini 共用同一份 SDK，只準備一次。這個準備動作只匯入本機模組，不建立 HTTP 用戶端、不送出 API 請求、不建立背景工作，也不修改供應商順序或金鑰。某個選用 SDK 無法預載時，會記錄其錯誤類型，保留原有啟動與容錯流程。

這改善的是「服務已上線後，第一筆翻譯才載入 SDK」的等待。必要載入工作被提前完成；若一則訊息剛好喚醒休眠主機，使用者仍需等待主機啟動，這項修改不能消除 Render 休眠喚醒成本。

**實際離線量測**

使用已安裝的真實 SDK、兩個獨立 Python 程序，封鎖外部網路；僅建立本機用戶端及取得方法，沒有呼叫模型。數字為單次本機量測，不是 Render 的秒數，也不是模型速度保證。

| 項目 | 原行為 | 新行為 |
| --- | ---: | ---: |
| 啟動階段的 SDK 資源準備 | 0.000 ms | 477.668 ms |
| 第一筆的用戶端與 SDK 準備合計 | 492.894 ms | 20.511 ms |
| 其中，第一筆 SDK 資源載入 | 473.435 ms | 0.023 ms |

以上證實可將首次載入移出翻譯請求，但不能斷言正式日誌的 4.506 秒全部是這個原因。新版本已補齊分段計時，供下一筆正式資料確認。

**新增日誌**

啟動時搜尋 `[SDKPrepared]`，確認需要的 SDK 出現 `status=loaded`。其中的 `http_requests=0` 只指這個本機準備步驟。

翻譯時搜尋 `[AIProviderPerf]`，以同一個 `trace` 對照原有 `[AIRequest]` 與 `[DeliveryPerf]`：

| 欄位 | 代表的時間 |
| --- | --- |
| `client_setup_ms` | 讀取／取得用戶端，包含可能的首次建立與設定鎖等待 |
| `request_options_ms` | 準備單次 SDK 的期限與重試選項 |
| `resource_setup_ms` | 取得翻譯方法，包含 SDK 延後載入 |
| `sdk_ms` | 實際 SDK 呼叫合計 |
| `sdk_logging_ms` | 寫入 SDK 請求結果紀錄 |
| `other_ms` | 同一供應商流程內其餘時間 |
| `outside_sdk_ms` | 供應商流程總時間減去 SDK 時間，供快速定位；不可再加入上述分段總和 |

若 `resource_setup_ms` 降低而 `outside_sdk_ms` 仍高，就能從其他欄位區分用戶端、記錄或其餘處理，無須再把所有等待統稱為 API 慢。

**更新與驗證**

將本包的 `app.py` 與 `ai_provider.py` 一起覆蓋專案根目錄並同次部署。其他檔案為新增測試與驗證說明，本包不包含未變動檔案。沿用現有設定即可。

最終完整離線回歸：2,948 項測試及 443 項子測試通過，耗時 84.89 秒。新增 11 項測試涵蓋本機準備、重複載入、供應商狀態保留、選用 SDK 失敗、實際模組匯入、計時歸屬、錯誤傳遞與重試期限。原有翻譯品質、數字人名保護、快取、持久化送達及重試測試也一併通過。

本包尚未部署到您的 Render；測試未呼叫付費 API、未傳送真實 LINE 訊息。實際縮短多少秒，請以上線後相同模型的正式日誌確認。
