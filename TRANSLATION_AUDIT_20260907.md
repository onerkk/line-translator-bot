# LINE 翻譯程式審查與修正（2026-09-07）

這次確認：錯譯不只來自 AI。程式中的固定句子捷徑、翻譯後替換及品質檢查，也會自行改錯意思或拒絕正確譯文。本包修正已重現的問題。

## 基準與驗證方法

- 基準為 GitHub `onerkk/line-translator-bot` commit `6b399d895a6974fd9ecc03f128c02301835f4b55`。下載清單中的 315 個原始檔案與本機基準逐一核對 Git blob 雜湊相符。
- 使用真實 `app.translate()` 流程及現有檢查器；僅替換 AI 與 LINE 的網路傳輸。測試提供預先撰寫的合理譯文，確認程式不會把它改錯、丟棄或用不相干的固定句子取代。
- 新增 82 項回歸測試。將其中 25 項放回未修正的基準執行，結果為 **21 項失敗、4 項通過**；包含 17 個完整流程錯譯／空白回覆反例，以及 4 個完成狀態判斷反例。
- 修正後完整發布測試：**1,437 項測試、438 項子測試通過，0 失敗、0 錯誤**。
- 工廠翻譯素材驗證通過：30 個正式案例全部接受，29 個禁止譯法探針全部拒絕；素材驗證無錯誤、無警告。

## 已確認與修正的問題

| 問題 | 原版本實際行為 | 修正後的處理 |
|---|---|---|
| 否定被短句捷徑吃掉 | `barang tidak rusak` 被送出為「料件損傷」 | 固定翻譯必須涵蓋整句；未處理的否定、數量、時間或動作交回正常翻譯 |
| 加工前檢查被固定句子覆蓋 | 「加工前後端沒有問題」變成「加工前後端就已有問題」；附加「請檢查」會消失 | 加工前狀態也執行完整原文覆蓋檢查 |
| 吊運、儲放指令被關鍵字誤觸發 | 「不要把料吊去品保」變成已被 QC 搬走；「照片位置不能放」變成可以放 | 移除不完整的品保固定句捷徑；儲放捷徑只接受完整、已支援的指令結構 |
| 疑問句變陳述句 | `I15 rusak?` 直接送出「I15 機台故障」 | 舊捷徑與來源關係翻譯器都保留疑問語氣，疑問句進入正常翻譯 |
| 正確詞義被跨語境替換 | 津貼與加班費合併；「已完成」變成「容易處理」；被偷走變成先搬走；化學反應變成回報；吞入變成消失；短尺規變成短料；城市變成工廠 | 替換必須有原文依據；遇到並存詞義時保留原譯文；拉丁詞替換增加完整詞邊界 |
| 正確常用詞被全域封鎖 | 工作表的 `lembar kerja` 被當成錯誤工單譯法，導致空白回覆 | 禁止譯法只對原文實際出現的詞條生效，保留工作表、確認資料等一般用法 |
| 尚未修好被判成已完成 | `belum selesai diperbaiki` 的 `selesai` 蓋過 `belum`；「維修沒有完成」的否定也可能漏判 | 完成詞受前方否定支配；中印尼雙向檢查區分已完成與未完成 |
| 儲放檢查只接受固定寫法 | 主動式 `tempatkan … bersama` 與「進入儲存區」的合理變體被拒絕 | 保留客戶名、代碼、儲放與集中等必要意思，接受合理主被動式與同義表達 |
| 舊結果可能再從快取返回 | 修好程式但舊翻譯仍可能留在快取 | 翻譯規則版本納入快取識別；舊版本結果失效，現行合格結果仍可重用 |

既有套環、ERP 原因欄、PMI、數量、班別、工單、跨群組隔離、圖片文字共用翻譯流程、作業確認、快捷選單、LINE 傳送與重送測試都包含在完整測試中。發布版本鎖定測試同步更新，保留原有行為斷言。

## 速度與費用

完整且可驗證的簡短狀態仍有不呼叫 AI 的路徑；新測試也確認合格結果第二次可使用快取。本次沒有調高預設模型、增加固定翻譯輪數或改變費用設定。

以前會被錯誤捷徑直接輸出的複雜句，現在可能需要正常 AI 翻譯；不能把錯譯的低延遲當成優化。這次也移除幾類誤攔截，避免正確譯文因此反覆重試。實際線上時間與帳單仍需部署後量測，離線測試不是速度或模型準確率的實測。

## LINE 官方介面核對

已將相關傳送程式與官方規格核對，並保留對應回歸測試：

- 一般文字長度使用 UTF-16 單位計數；不是所有介面欄位都採相同計數方式。[LINE 字元計數規則](https://developers.line.biz/en/docs/messaging-api/text-character-count/)
- 每次傳送最多 5 個訊息物件；快捷回覆最多 13 個按鈕。[訊息傳送](https://developers.line.biz/en/docs/messaging-api/sending-messages/)、[快捷回覆](https://developers.line.biz/en/docs/messaging-api/using-quick-reply/)
- 可重試的推播使用穩定的 `X-Line-Retry-Key`；回覆 API 不支援這個機制。[重試 API 請求](https://developers.line.biz/en/docs/messaging-api/retrying-api-request/)
- Webhook 重送需要事件去重；重送順序可能不同。[接收訊息與 Webhook 重送](https://developers.line.biz/en/docs/messaging-api/receiving-messages/)

## 更新與手機驗收

1. 解壓縮更新包，把其中全部檔案放到 GitHub 專案根目錄並覆蓋同名檔案。這是變更檔案包，不是完整專案；不要刪除原有其他檔案。
2. 一起更新程式、知識檔和測試，不要只上傳 `app.py`。確認 GitHub 的 Factory translation release gate 通過、Render 部署顯示 Live。
3. 在測試群組重新送出下列訊息，檢查新回覆。歷史 LINE 訊息不會被改寫。

| 測試原文 | 必須保留的意思 |
|---|---|
| `套環要補上` | 補裝套環／保護環，不能變成重秤毛重 |
| `barang tidak rusak` | 料件沒有損傷 |
| `I15 rusak?` | 詢問 I15 是否故障，不能斷言已故障 |
| `Mesin I9 belum selesai diperbaiki` | I9 尚未維修完成 |
| `不要把料吊去品保` | 禁止把料移往品保，不能改成已經移走 |
| `夜點費與夜班加班費分開計算` | 夜班津貼與夜班加班費是兩個不同項目 |

本次沒有向正式 LINE 群組發訊息，也沒有呼叫付費模型或部署到 Render。上述測試證明已重現的程式問題被修正，不能證明未見句子、模糊現場術語或辨識錯誤的照片永遠翻譯正確。
