# CI #86 修正版

這份 ZIP 是前一份「指令確認卡與未回覆提醒」更新之後的增量修正。解壓後，將 6 個修改檔依原相對路徑覆蓋至專案，再提交讓 GitHub Actions 重新執行。

## 原因與修正

使用原版本重現了回報的全部 5 項失敗。主因是改成指令觸發確認卡後，有 3 組舊測試仍要求普通翻譯自動建立確認紀錄；其中 2 組分別測試 SQLite 與 Redis，因此共失敗 5 項。

- Flex 保留測試現在分別驗證普通翻譯與明確建立的確認卡：普通翻譯不附確認按鈕；確認卡保留原內容、同一個 token 和正確的按鈕。
- 翻譯效能測試確認普通翻譯只有原文版本及操作上下文的 2 次原子寫入，不再要求多做確認紀錄的讀寫。同時保留重送去重、翻譯快取與送達檢查。
- 圖片上下文測試涵蓋有、無確認紀錄兩種情境；不再對本來就不應存在的紀錄展開字典。已有確認紀錄時，仍檢查延後補入原文版本不會覆寫回覆。
- 正式程式也移除普通圖片翻譯對不存在確認紀錄的多餘查詢，既有確認紀錄仍可補入原文版本。
- 後續 DOM 發布檢查另發現預設按鈕減少後，原本假設「圖片選單固定兩頁」的測試會失敗。測試改為建立 14 顆具有不同內容的按鈕，驗證第一頁 12 顆、第二頁 2 顆，以及換頁操作。
- 補測指令卡預覽、底部選單關閉時的指令卡、後台提醒時間儲存及群組間的設定區隔。
- 離線檢查工具改為使用 pytest 9 內建的 subtests；舊版 pytest 才載入獨立插件。

## 實際驗證結果

| 檢查 | 結果 |
| --- | --- |
| 修正前回報案例 | 完整重現 5 項失敗 |
| 修正後相關回歸 | 138 passed、5 subtests passed |
| 全部 Python 回歸 | **1815 passed、443 subtests passed**，51.70 秒 |
| 版本化工廠資產檢查 | 通過，errors=[]、warnings=[] |
| 前端 JavaScript 語法 | 通過 |
| 工廠頁面 DOM 操作檢查 | 通過，包括提醒設定、回覆紀錄、群組切換、設備、表單、QR、分享 |
| 統一選單 DOM 操作檢查 | 通過，包括指令卡、條件顯示、排序、刪除、繼承、分頁及儲存失敗回饋 |

環境：Python 3.12、pytest 9.1.1、Flask 3.1.3、LINE SDK 3.25.0、fakeredis 2.37.1（Lua）、jsdom 30.0.1。測試使用臨時資料庫及模擬 LINE／AI 傳輸；完整 Python 回歸另外封鎖外部網路。以上為本機發布檢查結果，GitHub Actions 會在你提交檔案後重新執行；本次沒有對正式 LINE 群組發送訊息。

## 修改檔

1. `line_factory_features.py`
2. `test_line_factory_features.py`
3. `test_line_translation_performance.py`
4. `run_translation_offline_checks.py`
5. `tests/factory_ui_smoke.cjs`
6. `tests/unified_menu_ui_smoke.cjs`

`/ack 通知內容`、`/確認 通知內容` 與後台未回覆提醒設定繼續使用。
