# 作業確認修正版

版本：2026-09-07.factory-receipts.4

以 GitHub `7c427fb21287614ddf819df0e442c2b4365175de` 為修改基準；開始修改時，工作檔與該版本的所有檔案雜湊一致。本 ZIP 僅包含本次修改／新增的 9 個檔案，保留原有翻譯品質、速度與成本優化。

## 改了什麼

- **通知有固定確認按鈕**：把「了解／說明／確認」放進翻譯卡片；純文字翻譯附加一張確認卡。群組有其他訊息後，仍可回到通知卡操作。保留既有翻譯快捷按鈕。
- **發起人能在原群組看見回覆**：成功儲存後，機器人回覆誰已了解、誰需要說明、通知摘要、發起人及目前確認名單。回覆卡也能再次查詢最新狀態。同一使用者更改回覆會更新原紀錄，不增加一個人。
- **後台群組清楚可選**：在「作業確認紀錄」旁直接選擇查詢群組，與上方設定群組同步，記住最後選擇。空結果會寫出群組名稱，查詢失敗另顯示錯誤，不會冒充零筆紀錄。
- **回覆時間與更新狀態**：後台顯示發起人、通知時間、回覆者與時間。開著紀錄區時每 20 秒更新，切到背景或離開該區時停止自動查詢；可隨時按「查看最新確認」。
- **修正 30 分鐘與 7 天期限不一致**：作業確認原本被翻譯樣式按鈕的 30 分鐘期限阻擋。現在確認直接查自己的紀錄，自原通知建立起保留 7 天；樣式翻譯控制維持原期限。已過期、原文更新／收回、其他群組的確認不接受。
- **補齊失敗處理**：有效操作資料存在而初始確認紀錄缺失時，可重建後再寫入。儲存尚未確認成功會明確回覆，並保留 webhook 重送重試機會；未收到 LINE 使用者身分時不建立匿名的成功紀錄。
- **避免重複處理**：原始 webhook 重送不重複通知；較舊的回覆事件不能覆蓋較新的狀態。確認回覆不再被當成一則新通知重新附加控制。

這個功能記錄「主動表示了解／需要說明」，不代表 LINE 個人已讀、不代表工作已完成。「需要說明」會請發起人在群組協助，不會自動產生作業教學或私訊。

## 更新後怎麼測試

1. 將 ZIP 解壓，按原有相對路徑更新到儲存庫，等待 Render 部署完成，再重新載入後台。
2. 「工廠工具 → 群組翻譯與互動」選擇實際 LINE 通知所在群組，例如「研磨C班」。第一次測試將「作業確認按鈕」選成「所有翻譯顯示」，按「儲存群組設定」。
3. 在同一個 LINE 群組傳送「【確認功能測試】請確認已收到通知。」若群組設定為只翻譯 @ 機器人的文字，測試時也必須真正點名機器人。
4. 翻譯後應出現含「了解／說明／確認」的卡片。這時後台選同一群組，應能查到該通知，尚未回覆時人數為 0。
5. 按「了解」，群組應出現帶姓名與通知摘要的確認回覆。後台按「查看最新確認」，或停留在紀錄區等候更新，應顯示該人的回覆與時間。
6. 另一位同事可回到同一張通知卡按「說明」，再按「確認」看兩人的最新狀態。

舊訊息不會自動增加固定按鈕，請用更新後新傳送的通知驗證。未成功寫入且操作資料也已過期／遺失的歷史紀錄，無法憑空補回。後台未回覆名單僅涵蓋機器人已知成員。

## 驗證與限制

- 全部 Python 發版檢查：**1277 passed，438 subtests passed**。
- 新增完整確認流程測試，涵蓋 SQLite、執行 Lua 的 Redis 模擬環境、真正驗簽的 webhook、逾期、群組隔離、遺失初始紀錄、儲存失敗、重送與身分缺失。
- 後台 DOM 操作驗證通過：切換群組、重新開頁保留群組、實際操作寫入後查詢、顯示回覆時間、可見區更新及背景停止查詢。原有掃码、站別翻譯、分享與表單流程亦通過。
- 工廠資產驗證通過，無錯誤與警告；前端 JavaScript 語法檢查通過。
- 按「了解／說明／確認」不呼叫 AI。原有一般翻譯的儲存往返次數與單次模型呼叫回歸測試仍通過。LINE 回覆或失敗後的 push 仍依既有傳送機制及方案計量。
- LINE 傳送及 AI 使用測試替身，未向真實群組發訊、未呼叫付費模型、未修改 Render 或 GitHub。仍需部署後以實際群組驗證。

截圖本身不足以證明「當時零紀錄」一定由哪一個原因造成：仍可能涉及當時所選群組、儲存狀態或 webhook 是否收到。這版修正的是已重現的期限、介面與回饋缺口，並補強失敗處理與紀錄可觀察性。

LINE 官方依據：
- [Quick Reply 消失條件](https://developers.line.biz/en/docs/messaging-api/using-quick-reply/#quick-reply-buttons-disappear)
- [Flex Message 互動訊息](https://developers.line.biz/en/docs/messaging-api/using-flex-messages/)
