# 作業確認未回覆提醒修正

這次依最新要求，只修正未按了解者沒有收到標記提醒的問題；確認卡按鈕設定沿用目前版本。

問題原因

截圖顯示機器人只記得發起人，已知未回覆者為 0。旧程式在 LINE 群組成員 ID API 不可用時，會使用不完整名單；名單沒有待提醒者就直接結束排程，沒有向其他人發送提醒。

此外，Flex 卡片中的 @All 或 @姓名只是一般文字。原本 /ack 指令沒有另送原生標記訊息，所以即使原始訊息帶有真正的 LINE 標記，確認卡也不會保留標記效果。

本次變更

- 提醒到期前重新查詢成員名單與群組人數，並合併機器人後來知道的成員。
- 有足夠成員身分時，以 textV2 的 user mention 標記未回覆者，排除發起人、已回覆者及已知離群者。
- LINE 仍未提供完整身分、或實際人數無法確認時，使用原生 @All 提醒一次。此備援也會標記已回覆者，訊息會請已回覆者忽略。
- 如果補查的人數及回覆紀錄已足以確認沒有未回覆者，停止提醒，不發送 @All。
- 保留原通知的到期時間、一次提醒、持久儲存、重啟恢復與相同請求識別碼重試機制。
- 通知中的有效 LINE 原生標記會另以 textV2 傳送。僅手動輸入 @All 文字、而沒有 LINE 標記資料的訊息，仍視為普通文字；到期備援提醒則由程式建立真正的 @All。
- 群內查詢及後台不再把不完整名單的 0 人視為全員了解；後台顯示名單補查時間、未知人數及個別標記／@All 的提醒結果。
- 更新後台腳本版本，讓重新開啟後台時載入新的狀態顯示。

LINE 官方依據

完整群組成員 ID 清單限認證或 Premium 官方帳號使用；不能因為 API 權限不足就認定群組沒有其他人：
https://developers.line.biz/en/docs/messaging-api/getting-user-ids/#get-all-user-ids-of-group-and-multi-person-chat-members

群組人數 API 回傳的人數不包含機器人本身：
https://developers.line.biz/en/reference/messaging-api/#get-members-group-count

textV2 支援原生 user 與 all mention；Flex 文字本身沒有這個標記欄位：
https://developers.line.biz/en/reference/messaging-api/#text-message-v2

更新檔案（依原路徑覆蓋）

儲存庫最外層：
- app.py（僅更新後台腳本版本參數）
- line_ack_reminders.py
- line_factory_features.py
- test_ack_reminder_roster.py

static 資料夾：
- static/admin_factory.js

tests 資料夾：
- tests/factory_ui_server.py
- tests/factory_ui_smoke.cjs

使用 GitHub 網頁上傳時，請分別進入上述資料夾上傳對應檔案；檔案必須覆蓋原本相同路徑。

更新後的實機驗證

1. 完成部署後，關閉並重新開啟後台。
2. 在工廠工具選擇正確群組，勾選自動提醒，設為 1 分鐘並儲存；連線與功能狀態的作業確認排程應顯示已啟用。
3. 在同一群組發一筆新的 /ack 測試。讓發起人按了解，另一位同事先不按。
4. 通知送達滿 1 分鐘後，排程會在下一次檢查處理（平常每 15 秒檢查，另外加上 LINE 查詢及傳送時間）。名單足夠時應個別標記未回覆者；名單仍不足時應收到真正的 @All。
5. 到後台查看該筆通知，確認顯示已完成個別 @ 提醒或已用 @All 補提醒一次。
6. 第二筆新通知改成所有已知對象先按了解；若名單與群組人數已核對完整，應不再提醒。

提醒時間在建立通知時記錄。請先儲存設定，再發新通知；舊版已結束為「沒有未回覆者」的通知不會自動重新群發。

驗證結果

- 更新前重現只有發起人時排程不發送的錯誤。
- SQLite 與 Redis 皆驗證空名單、補查恢復、稍後取得成員、真正原生標記、已回覆排除、未知人數、空 ID 清單、模糊網路結果重試及群組無新訊息時的背景排程。
- 本機完整 Python 回歸：1841 passed, 443 subtests passed，53.29 秒。
- 資產檢查：ok=true，errors=[]，warnings=[]。
- 前端六項操作流程通過：確認紀錄、管理頁、成員頁、表單、提醒結果、快捷選單分頁。
- 本機測試使用假 LINE 傳輸與受控時間；未向正式 LINE 群組發送測試訊息，正式手機收到通知的結果仍需依上述步驟確認。
