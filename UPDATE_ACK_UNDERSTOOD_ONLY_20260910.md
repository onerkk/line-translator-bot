本次更新：移除「需說明」回覆分類，只有「了解」才算完成
版本：2026-09-10.8-understood-only

套用方式

將壓縮檔內檔案依原路徑覆蓋至目前專案，再重新部署／啟動服務。
static/admin_factory.js 必須放在 static 資料夾；後台請重新整理。
line_factory_features.py、line_ack_reminders.py、line_factory_store.py 必須一起更新。
此為本次變更檔，請套用在先前已更新的專案上，並保留現有資料庫與設定。

修正內容

- LINE「查看回覆」與後台回覆查詢只列已了解、未回覆，不再列出「需說明」人數、名單或「請發起人協助說明」文字。
- 舊 needs_help 紀錄一律視為尚未按了解。原本已了解的人維持完成；發起人、未指定人員與已離群人員仍依原規則排除。
- 到提醒時間，排程會重新核對尚未按了解的對象，包含以前誤按「需說明」的人，再個別 @ 標記。
- 舊版本若因把 needs_help 算成完成而提前自動停止通知，背景排程會分頁修復；不受後台最近 100／200 筆查詢上限限制，也不必先開啟後台。
- 修復保留原訂提醒時間與間隔，不因查詢或按鈕操作立即重貼公告。曾送過或可能送過的舊提醒會等待下一個間隔，並使用新的請求識別碼。
- 手動停止、設定關閉、通知過期、原文失效及機器人退群等情況不會被重新啟用；已完成單次提醒的通知也不會變成持續提醒。
- 舊 needs_help 原始紀錄保留供程式相容，但不當成已回覆，也不出現在查詢回覆資料；使用者有效按下了解後才更新成 understood。
- 舊卡片的 factory_help 操作直接忽略，不查詢或建立通知、不更新回覆、不發群組訊息或私人確認。其舊時間戳也不能擋住真正的了解回覆。
- 新通知、提醒卡與文字／圖片翻譯快捷選單已驗證不會產生「需說明」按鈕，即使舊設定曾將它開啟。已送出的歷史卡片外觀不會因覆蓋程式而被改寫；其舊按鈕更新後按下無效。
- 了解仍保留每人每則通知的一次性私人確認，不重貼公告。查看回覆、停止提醒與照片詢問的既有發起人／管理員權限維持不變。

驗證結果

已先用舊程式重現錯誤：14 個回歸案例失敗，包括 needs_help 被排除、舊按鈕仍讀寫資料，以及誤停通知沒有排程。
修正後完整離線回歸：2,613 項測試與 443 項子測試通過。
實際後台 DOM、快捷選單、回覆查詢、表單與群組設定測試通過；翻譯資產檢查通過。
SQLite 與模擬 Redis 皆驗證舊資料、到期前後、並行了解／手動停止、分頁與寫入失敗重試。
測試封鎖對外 LINE／AI 連線，未向正式群組發訊息。本檔不代表已替正式服務部署或完成線上驗證。

可重跑檢查

    python run_translation_offline_checks.py
    python validate_factory_translation_assets.py --json
    python tests/run_factory_ui.py

官方技術依據

- [LINE Postback action](https://developers.line.biz/en/docs/messaging-api/actions/#postback-action)：按鈕資料送至 webhook，由伺服器決定後續行為；退役操作在入口直接忽略。
- [Redis SCAN](https://redis.io/docs/latest/commands/scan/)：分頁遍歷可能回傳空頁或重複項目；修復流程持續處理游標、容許重複，寫入失敗後重試。
