# 自動累積名單、持續提醒、停止提醒與 CI #91 修正

本次依最新要求保留原本的了解／確認按鈕設定，新增停止提醒控制。

如何使用

1. 依本包路徑覆蓋檔案，完成部署後關閉並重新開啟後台。
2. 工廠工具 → 選擇群組 → 勾選「自動 @ 提醒尚未回覆的人」。
3. 設定提醒間隔，例如 1 分鐘，勾選「持續提醒，直到全員回覆或手動停止」，儲存群組設定。
4. 發一筆新的 /ack 通知內容。首次及後續提醒使用設定的間隔；每輪補查名單、排除發起人、已回覆者及已知離群者。排程通常每 15 秒檢查，再加上 LINE 查詢及傳送時間。
5. 名單足夠時個別 @ 未回覆者；仍有無法辨識的成員時，該輪使用真正的 @All，已回覆者也會收到。
6. 可確認全員已回覆時會停止；也可手動停止，最長不超過原通知的 7 天有效期。

如何關閉

- 單筆通知：發起人可按確認卡的「🛑 停止提醒/Stop」。有工廠工具權限的 LINE 管理員也可操作。
- 後台單筆停止：工廠工具 → 作業確認紀錄 → 「停止此通知提醒」。需已登入且有工廠工具管理權限。
- 關閉整個群組：取消「自動 @ 提醒尚未回覆的人」並儲存；之後不會發送該群組待處理的提醒。
- 只提醒一次：建立新通知前取消「持續提醒」並儲存。
- 停止提醒後，原通知與已收到的了解回覆仍保留，可繼續查看確認狀態。

新的提醒間隔及持續模式在建立通知時記錄。請先儲存設定，再發新的 /ack 通知；舊版已完成一次提醒的通知，不會在更新後突然重新群發。

名單如何自動取得

- 記錄 LINE 提供的發言者、加入成員、確認卡操作人，以及原生 @ 單獨標記對象的 ID。
- 即使當時查不到姓名，也先記住 ID，之後可用原生標記提醒該人。
- 使用既有本機持久紀錄及姓名快取保存名單，提醒前重新讀取，避免只依賴某一個程序的記憶體。
- 後台新增「已辨識的群組成員」，可查看目前記得的名單。
- 尚未辨識的同事可在群組發一個字；或由您使用 LINE 的 @ 選人功能逐一標記。原生 @All 只代表全體，不包含每人的 ID 清單。

完整名單 API 的權限限制仍由 LINE 決定。對完全沒互動、未被個別標記，而 LINE 又未提供 ID 的人，程式無法憑姓名猜出其身分。
[LINE 官方：取得使用者 ID](https://developers.line.biz/en/docs/messaging-api/getting-user-ids/#get-all-user-ids-of-group-and-multi-person-chat-members)

CI #91 的處理

- 在隔離副本中，以新版 UI 測試搭配舊版提醒核心，重現相同的 no_pending／sent_all 斷言失敗及後續 45 秒逾時。此實驗可說明一種成因；未直接讀取該次 GitHub 提交，不能據此確認線上檔案組合。
- 提醒 UI 案例改為建立獨立通知，明確安排一位未知成員，不共用前面流程的通知狀態。
- 新增核心版本檢查；若 line_factory_features.py 與 line_ack_reminders.py 不匹配，明確指出需一併覆蓋的檔案。
- UI 測試成功或失敗都會關閉瀏覽器視窗及計時器；測試伺服器使用獨立埠，並在失敗時輸出診斷。
- 核心混用的驗證案例以退出碼 1 在 1.28 秒結束；刻意觸發 DOM 斷言的驗證案例以退出碼 1 在 1.65 秒結束，均保留真正錯誤、沒有第二次逾時。

本機驗證

- 1865 passed, 443 subtests passed，52.84 秒。
- 資產檢查 ok=true，errors=[]，warnings=[]。
- 前端六項流程通過：確認紀錄、管理頁、成員頁、表單、提醒與停止控制、快捷選單。
- SQLite／Redis 皆涵蓋連續多輪、回覆後排除、成員後來發言、重啟讀回名單、每輪識別碼、同輪重試、到期、單筆停止、管理權限與群組關閉。
- 一般翻譯的效能測試仍通過；名單記錄不新增一般翻譯的雲端讀取。
- LINE 傳輸及時間使用測試替身；未向正式群組發送測試訊息。本機通過不等於已部署或已確認手機通知。

覆蓋路徑

儲存庫最外層：
- app.py
- line_ack_reminders.py
- line_factory_features.py
- test_line_ack_commands.py
- test_ack_repeat_controls.py

static 資料夾：
- static/admin_factory.js

tests 資料夾：
- tests/factory_ui_server.py
- tests/factory_ui_smoke.cjs
- tests/unified_menu_ui_smoke.cjs
- tests/run_factory_ui.py

本包只有上述 10 個變更檔及此說明。使用 GitHub 網頁上傳時，請分別進入正確資料夾上傳、覆蓋原路徑，並查看新提交的 CI 結果。
新版 UI 檢查紀錄應出現：CHECK fixture modules: 2026-09-09.ack-repeat-stop.3
