# ACK119：重新提醒時，只標記已知未回覆者

接續 ACK118。本 ZIP 僅包含本次變更檔案。

## 本次行為

- 每一輪提醒依最新回覆紀錄，使用真正的 LINE @ 個別標記已知未回覆者，並附上原通知的確認卡。
- 名單完整性尚未確認，或 LINE 顯示仍有其他未知成員，也只標記已有 ID 的未回覆者，不再自動改用 @All。
- 例如已有 11 筆回覆、剩下 5 位未回覆，提醒只標記這 5 位；其中 1 位回覆後，下一輪只標記剩下 4 位。
- 超過 20 人會分批處理，每一批重新排除已回覆及已離群者，同一輪不重複標記同一人。LINE 官方每則訊息最多可替換 20 個原生標記。[LINE 官方訊息規格](https://developers.line.biz/en/reference/messaging-api/#text-message-v2)
- 已知未回覆為 0 立即自動停止，保留 ACK118 的停止規則、手動停止及群組開關。
- 發起人、本機器人與已離群者不列入待回覆；原始通知內容及翻譯保留。

原始 /ack 通知若由使用者選擇真正的 @All，初次發送仍保留；後續自動提醒使用未回覆者的個別標記。確認卡引用原文中的「@All」只是文字，提醒對象以另外發出的原生標記訊息為準。

## 既有通知與傳送重試

既有的持續提醒會自動採用新規則，不必重新發起通知。

若傳送失敗後，有人已回覆或離群，會取消原本包含該成員的重試請求，在設定的提醒間隔後重新核對待回覆者，再以新的請求識別碼送出個別標記。舊版待重試的 @All 也會取消並依同樣方式處理；未回覆已歸零則直接停止。

尚未開始傳送的訊息若剛好遇上回覆，會立即重新篩選對象。已經交給 LINE 的請求無法撤回。對象未變的網路重試仍保留原訊息及相同識別碼，避免重複處理；這依循 LINE 對重試內容及對象不可任意更改的要求。[LINE 官方重試規範](https://developers.line.biz/en/docs/messaging-api/retrying-api-request/)

## 套用

1. 將 ZIP 內全部檔案依原路徑覆蓋到目前專案，保留 static/ 與 tests/ 資料夾。
2. line_ack_reminders.py 與 line_factory_features.py 必須一起更新；兩者版本皆為 2026-09-09.ack-pending-mentions.5。
3. app.py 僅更新後台 JavaScript 的瀏覽器快取版本；static/admin_factory.js 更新提醒方式說明及狀態文字。
4. 提交後確認 CI 通過，再依既有服務流程部署及重啟，後台重新整理一次。

這是差異更新，須在已套用前次更新的專案上使用。不要再執行舊版更新安裝器覆蓋本次檔案。本 ZIP 不包含金鑰、資料庫或群組設定。

## 驗證

- 完整離線回歸：2071 項通過、443 個子測試通過。
- 新增 18 項實際訊息測試，涵蓋 SQLite 與 Redis：5 人變 4 人、44 人分批、即將送出時收到回覆、離群、舊版 @All 重試及到期取消。
- 所有介面測試通過，包含不完整名單下的個別標記、後台狀態及零人自動停止。
- 前端版本檢查、JavaScript 語法及翻譯資產檢查通過。

測試使用隔離資料、模擬 LINE 傳送及實際 DOM；未向正式群組發送訊息，未操作線上部署。
