# 翻譯費用、速度與語義修正

版本：`2026-09-07.factory-cp.3`。以 GitHub main `617463f23ee74902b52fb4ac52a9ce6f93d2d653` 為修改基準。

## 已修正的實際問題

1. **普通印尼文誤用高價模型。** 舊正規表示式會把一般英文字母單字算成設備代碼。現在代碼需包含字母與數字，保留真實工單、設備、數值、單位、多行及高風險判斷；印尼文警示詞也須符合單字邊界。
2. **無關範例增加費用並干擾翻譯。** 「今天下午再確認包裝進度」原先會帶入 BF2 保養、月底人力與 3800 目標的範例。現在一般時間詞和「確認」不能單獨啟用另一個流程；PMI、ERP 等縮寫仍可找回相關參考。原文條件、術語、資料保護與品質檢查保留。
3. **PMI 狀態漏判。** 補上「還沒／還没檢測、檢測尚未完成、檢測完成、不需檢測、檢測不要做」的狀態判斷，區分鋼種檢驗流程與檢查 PMI 儀器；保留疑問句、機台、編號項目及檢驗／包裝先後的檢查。
4. **混料禁令、停機／開機反向漏判。** 新對照測試找出「禁止混料、立即停機」被翻成允許混料、啟動機台仍可通過的漏洞。現在把材料混合與機台開停作為操作關係檢查，保留否定詞、指令與機台歸屬。
5. **範例搜尋重複計算。** 原文識別與中文內容片段採用有上限的純文字快取；同一句原文的識別與權重不再對每個候選重算。這些快取不儲存翻譯正確與否的決策；修改範例仍會立即生效，語義版本更新也會使舊翻譯快取失效。

簡單且狀態清楚的 PMI 短句仍走省費模型，本機檢查失敗後才使用既有的一次品質修正。多個不同檢驗狀態、先後順序、危險操作及複雜資料保留品質模型。維持每個翻譯工作的兩次生成上限與共用期限。

## 實測結果

同一套離線工具、相同 15 筆訊息、每筆範例搜尋重複 9 次；工具封鎖網路，AI 回覆使用預先寫好的測試譯文。

| 指標 | 修改前 | 修改後 |
| --- | ---: | ---: |
| 6 句普通印尼文中，誤升級的句數 | 5 | 0 |
| 包裝進度句送出的提示字元 | 5,089 | 3,168 |
| 全部 15 筆提示字元合計 | 68,635 | 65,285 |
| 本機範例搜尋耗時中位數 | 7.400 ms | 5.156 ms |
| 15 個合理測試譯文通過數 | 15 | 15 |
| 6 個刻意改錯的測試譯文攔截數 | 0 | 6 |

這些時間只代表本機範例搜尋，字元數不是帳單 token 數。結果不能換算成實際 LINE 回覆快幾秒、整體帳單省幾成，或模型真實準確率。這次沒有可用的付費 API 金鑰，沒有進行真實模型 A/B，也沒有修改線上服務。

驗證結果：**1,257 項 pytest 測試、438 項子測試通過**；工廠術語與翻譯資產檢查通過。新增 47 項回歸涵蓋一般印尼文分流、真實代碼與高風險分流、範例相關性、雙向 PMI 狀態、設備與流程區別、混料禁令及開停機相反指令。原有 12 組工廠流程改寫、否定句、資料／術語、供應商切換、重試與傳送測試亦通過。

## 使用方式

ZIP 只含本次修改及新增檔案。請按原路徑覆蓋，保留 `tests/data/` 子資料夾。覆蓋時一併更新 `app.py` 和本包其他程式檔，避免模組版本不一致。重新部署後，功能狀態的 build 應為 `2026-09-07.factory-cp.3`。

離線重現：

```bash
python benchmark_translation_cp.py --output cp-result.json
```

與修改前版本使用同一份工具、同一份資料比較：

```bash
python benchmark_translation_cp.py --repo /path/to/previous-version --output cp-before.json
```

原始對照記錄附於 `tests/data/cp_benchmark_20260907.json`。這份資料及測試不會加入機器人的翻譯捷徑或範例庫。
