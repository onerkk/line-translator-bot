# ACK120：重複按「了解」保持安靜

接續 ACK119。本 ZIP 僅包含本次變更檔案。

## 更新內容

- 同一人對同一則有效通知，第一次按「了解」正常記錄並回覆；之後重複按不再發訊息或重發確認卡。
- 原始卡片、重新提醒的卡片共用該通知的回覆紀錄，重按任何一張都不會洗版。
- 重複操作保留第一次回覆的時間與姓名，不重做顯示名稱查詢。
- 舊版已儲存的回覆也適用，服務重啟後仍能辨識。
- 連點及不同工作處理器同時收到操作時，以資料庫的原子更新確認哪一筆成功記錄，只有成功記錄者能發送回覆。
- 不同人、不同通知分別處理，不會擋住其他人的第一次確認。
- 「查看回覆」仍可主動查詢；已知未回覆歸零自動停止，以及每輪只標記未回覆者的功能保留。

若第一次儲存失敗，仍顯示原有失敗提示並允許重試；成功儲存後才視為已回覆。

## 套用

1. 將全部更新檔依原路徑覆蓋到已套用 ACK119 的專案。
2. line_factory_features.py 放在專案根目錄；tests/ 下的檔案放回 tests/。
3. 提交後確認 CI 通過，再依既有流程部署並重啟服務。無須重新建立群組設定或既有通知。

本次更新僅改動回覆處理與驗證測試，沿用 ACK119 的提醒模組相容版本。

## 驗證

- 全部離線回歸：2093 項通過、443 個子測試通過。
- 新增 22 項重複按鈕測試通過，包含同時寫入衝突、舊紀錄、獨立通知、群組關閉、儲存失敗重試及回覆失敗後的防重複。
- 所有介面測試通過，已確認第一次產生一筆回覆，後續重按及事件重送均產生零筆回覆。
- 翻譯資產、前端版本及變更 JavaScript 語法檢查通過。

測試使用隔離 SQLite、模擬 Redis，以及模擬 LINE 傳送；介面以實際 DOM 驗證。未向正式群組發送訊息，未修改線上部署。
