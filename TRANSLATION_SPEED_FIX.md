# 翻譯速度修正 — 2026-09-07

此差異更新包以目前 main 的 `dfd4ded54d341a20200a35861ed0bf6b058375ab` 為基礎，只包含本次新增或修改的檔案。

## 找到的延遲原因與修改

- 新增的訊息版本、互動按鈕及通知確認功能，原本逐項讀寫雲端，剛寫入的按鈕資料又立即讀回。現在以原子操作合併寫入，送出時重用已登記的按鈕資料，支援文字與 Flex 卡片。
- 雲端儲存使用可重用的 HTTP 連線；連線與讀取逾時分別設為 2 秒、3 秒。一次連線失敗後暫停該儲存連線 15 秒，避免同則訊息每個步驟都重新等待。翻譯保留本地版本紀錄；未成功儲存的確認功能不會宣稱已儲存成功。
- LINE 名稱與頭像共用查詢。成功查到「沒有頭像」也會暫存 10 分鐘，失敗暫存 15 秒；依群組、聊天室、私訊選擇 API，並設定連線與讀取逾時。慢速雲端查詢移出全群組共用的按鈕鎖。
- 同一翻譯工作內，相同內容、方向、範例、修正及詞庫的案例搜尋可以重用。資料有變更立即重新搜尋；每次品質檢查仍執行。
- 新增 `[DeliveryPerf]`，補足原本 `[Perf]` 未包含的翻譯前儲存及送出階段。

## 效能對照的實際範圍

使用你提供的五點工廠通知，經過實際訊息處理、語義檢查、佇列與 LINE 訊息封裝。Redis 執行真實 Lua；雲端每次往返模擬 100 ms。AI 與 LINE 傳輸使用替身，不會產生帳單或發送訊息。修正前後各測 5 次，表內時間為中位數。

| 項目 | 修正前 | 修正後 |
|---|---:|---:|
| 送出前雲端 HTTP 請求 | 10 次 | 2 次 |
| 整個處理流程雲端 HTTP 請求 | 12 次 | 4 次 |
| 到 LINE 傳輸替身接受的時間 | 1,251.1 ms | 379.3 ms |
| 整個處理流程時間 | 1,456.5 ms | 584.2 ms |
| 新通知主翻譯生成次數 | 1 次 | 1 次 |

以上不是你 Render 服務的實測秒數；不包含真實 AI 生成、LINE 網路傳輸或 Render 喚醒時間。實際改善須看部署後紀錄。HTTP 請求次數也不等於雲端服務帳單中的計費指令數。

## 費用與品質

保留現有模型選擇、必要品質修復、PMI／設備／站別詞義及否定、數字、條列完整性檢查。本次主要縮短附加功能等待，沒有降低新句主翻譯的模型單價。

已驗證：同一群組再發一次相同且合格的通知，會使用現行驗證快取，第二則不再呼叫 AI，但有自己的通知與確認紀錄。重送同一 webhook 也不重複生成或發送；只有 LINE 傳送失敗時，既有復原流程會重用已生成譯文。詞庫、修正範例或相關背景變更時，仍依原有規則重新驗證或翻譯，避免省費用卻沿用錯誤內容。這些用量控制原本已有，本次持續保留並驗證。

## 驗證結果

- 完整 Python 回歸：1,210 項通過，438 項子測試通過。
- 工廠詞庫與翻譯資產檢查通過。
- 管理後台、成員工廠工具、分享／取消分享、掃碼失敗、表單驗證與重複提交的 DOM 操作測試通過。
- 新增回歸涵蓋：雲端請求次數、重複通知的 AI 呼叫數、併發確認資料保留、原文修改／收回／重啟後版本確認、Flex 按鈕重用、儲存斷線、個人資料查詢與範例搜尋快取隔離。

## 更新與確認

1. 將 ZIP 解壓後的 7 個檔案上傳到 GitHub 儲存庫根目錄，覆蓋同名檔案。
2. 等待 GitHub「Factory translation release gate」通過，以及 Render 完成同一次提交的部署。
3. 在 LINE 測試群組發送五點通知，確認譯文完整、PMI 表達為執行檢驗的流程，並測試「了解」「說明」等確認按鈕。
4. 在 Render Logs 搜尋 `[DeliveryPerf]`。一般文字通知的模擬測試結果為 `storage_calls=4`；其他訊息形式、舊按鈕或復原情境可能不同。
5. 同時看 `[Perf]` 的 `api` 耗時。`[DeliveryPerf]` 的 `sent` 是此程序開始處理至 LINE 接受完整發送的時間；`total` 包含後續確認狀態同步；`storage` 是新增工廠互動儲存的 HTTP 耗時。`sent=none` 表示該次沒有記錄到接受發送，可能是重送、被較新版本取代或尚在復原佇列，須搭配其他日誌判斷。

本次工廠工具版本識別為 `2026-09-07.factory-speed.2`。若翻譯仍慢，新的兩組耗時紀錄可區分 AI、互動儲存及其他處理，避免再只憑感覺修改。

技術參考：[Upstash REST API](https://upstash.com/docs/redis/features/restapi)、[Requests 連線重用](https://requests.readthedocs.io/en/master/user/advanced/)。
