# 自訂提醒：更新與使用

本 ZIP 只包含本輪新增或修改的檔案，套用於上一輪已修正的專案。將所有檔案依 ZIP 內相對路徑放到專案根目錄，保留 `static/` 與 `.github/workflows/` 目錄；請勿只覆蓋 `app.py`。重新部署後，後台「總覽」旁會出現「自訂提醒」。

## 如何設定

1. 登入後台，開啟「自訂提醒」。
2. 選擇機器人所在的群組。
3. 選擇日期與時間，固定使用台灣時間（UTC+8），不受瀏覽器或伺服器時區影響。
4. 選擇「所有人（@All）」、「指定成員」或「不標註」。指定成員可複選，最多 20 人。
5. 輸入內容、檢查預覽，按「儲存提醒」。

範例：日期 `2026-09-20`、時間 `08:00`、標註「所有人（@All）」、內容「要開班股會議」。儲存後，到該時間才派送；此更新包沒有建立任何範例排程。

這是單次提醒。尚未開始派送的提醒可以修改或取消。名單取自既有群組互動紀錄；找不到成員時，請讓該成員在群組發言，再重新整理。派送時機器人及指定成員仍須在群組內。指定成員已離群造成 LINE 拒絕時，紀錄會顯示失敗原因，不會改成未標註的訊息後宣稱成功。

內容照原文發送，不呼叫 AI、不增加翻譯 token；LINE 推播用量仍依帳號方案計算。這裡使用 LINE `textV2` 正式標註格式，並處理內容中的大括號，不是把 `@All` 當作一般文字。

## 首次啟用：保存提醒

優先使用既有的 Upstash：已設定下列兩個環境變數時，不必另外建立資料庫。

```text
UPSTASH_REDIS_REST_URL=https://你的資料庫網址
UPSTASH_REDIS_REST_TOKEN=你的存取權杖
```

若沒有 Upstash，可設定 SQLite 路徑，但必須位於部署平台提供的持久磁碟：

```text
REMINDERS_DB_PATH=/var/data/reminders.db
```

兩者同時存在時，固定使用 Upstash。連線失敗不會切換到另一份空資料庫，也不會把尚未保存的提醒回報為成功。未設定任何儲存時，後台會顯示原因並停用儲存按鈕。請勿將正式提醒放在 Render Free 的 `/tmp` 或部署目錄；這些不是持久磁碟。

同一個 Bot 的所有執行程序必須使用相同儲存。若多個不同 Bot 共用同一個 Upstash，請各自指定不同的 `REMINDERS_KEY_PREFIX`；建立提醒後不要任意更換此值或切換儲存。

## 準時派送與主機休眠

後端每 30 秒檢查一次到期提醒，正常運作時通常在預定時間後約 30 秒內開始派送。關閉管理後台不影響排程。主機停機、網路異常、LINE 額度限制或平台延遲仍可能影響派送時間。

Render Free 會在沒有流量一段時間後休眠。要在未收到群組訊息時仍執行提醒，請使用持續運作的主機，或設定外部排程定期喚醒。此 ZIP 包含喚醒端點，但不會替你開通外部服務或修改正式部署設定。

外部排程設定：

1. 在 Bot 主機設定 `REMINDERS_CRON_SECRET`，使用至少 32 字元的隨機密鑰，勿使用管理密碼。
2. 在你的外部排程服務設定每分鐘一次 HTTP **POST**。
3. URL：`https://你的-Bot-網址/api/reminders/tick`。
4. Header：`Authorization: Bearer 你的REMINDERS_CRON_SECRET`。
5. 不需要 request body。設定足夠的連線逾時，容納主機冷啟動。

端點回傳 HTTP `202` 表示已喚醒後端檢查排程，不代表提醒已送達。這個端點只能啟動已儲存且到期的提醒，不接受外部傳入的群組或提醒內容。密鑰錯誤回傳 `403`，未設定回傳 `503`。外部排程本身也可能延遲，不能保證精確到秒。

`REMINDERS_WORKER_ENABLED` 預設為 `1`。一般部署請保持預設；設為 `0` 會停用自動啟動，但持有正確密鑰的喚醒請求仍能啟動排程。

## 狀態與故障處理

已結束的提醒（LINE 已接受、派送失敗、需要確認收件、已取消）從最後狀態變更起保留 **7 天**，滿 7 天自動刪除內容及清單索引。既有紀錄也適用。等待提醒、派送中及等待重試的工作會繼續保留，包括很早以前建立的未到期提醒。

清理由後端排程自動執行，關閉後台不影響；每次分批清理，避免大量舊紀錄拖慢派送。開啟清單時也會清理，過期紀錄立即停止顯示。主機停機期間無法執行刪除，恢復後自動接續清理，不需另設排程或新增環境變數。

| 狀態 | 意義／處理方式 |
| --- | --- |
| 等待提醒 | 已保存，等待設定時間，可修改或取消 |
| 派送中 | 已領取處理，暫時不能修改或取消 |
| 等待重試 | 暫時連線或 LINE 額度問題，使用同一識別碼重試；可取消後續重試，但已送達訊息不會收回 |
| LINE 已接受 | LINE API 已接受該請求；不代表成員已讀 |
| 派送失敗 | LINE 拒絕訊息或權限等問題，請依錯誤原因修正，再新增提醒 |
| 需要確認收件 | 第一次派送後已超過 23 小時仍無法確認；停止自動重試，請先查群組，避免超過 LINE 24 小時防重複期限後再次發送 |
| 已取消 | 停止後續派送 |

主機重啟後會讀取持久化排程，補送尚未嘗試的過期提醒。派送中斷的工作在 120 秒租約到期後可恢復。多執行程序使用原子領取與同一筆 LINE 重試識別碼；LINE 回覆附有接受識別碼的 `409` 也會視為已接受。這些機制降低重複風險，不能提供跨平台永遠「只送一次」的保證。

## 管理員權限

超級管理員可直接使用；一般管理員需要在「使用者」頁面獲授「自訂提醒」。管理員請重新以 Google 登入一次。後台現在驗證登入簽發的憑證，單獨傳入使用者 ID 不再有效；憑證 12 小時後過期，需要重新登入。既有上傳及管理頁面的請求已一併更新。若有自行撰寫的管理 API 程式，也要傳送登入回傳的 `manager_token` 作為 `X-Manager-Token`，並保留 `X-Manager-Id`。

## 驗證

請在未載入正式 API 與儲存憑證的測試環境執行。CI 已配置離線測試用參數。

```bash
python -m pip install -r requirements.txt -r requirements-test.txt
REMINDERS_WORKER_ENABLED=0 python -m pytest -q test_*.py
python validate_factory_translation_assets.py --json
```

測試涵蓋台灣時區、@All／指定成員格式、真正 Lua 引擎執行的 Redis 原子操作、SQLite、並行領取、失敗重試、重啟補送、儲存失敗、管理員登入權限及前端 JavaScript 語法。測試使用隔離資料及模擬 LINE，不會向正式群組發送訊息。

官方規格：[LINE 標註訊息](https://developers.line.biz/en/reference/messaging-api/#text-message-v2)、[LINE 重試識別碼](https://developers.line.biz/en/docs/messaging-api/retrying-api-request/)、[Render Free 休眠與儲存限制](https://render.com/docs/free)、[Upstash Lua 原子操作](https://upstash.com/blog/lua-scripting-on-upstash-redis-atomic-operations-over-http)。
