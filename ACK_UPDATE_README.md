# 指令作業確認與未回覆提醒

版本：2026-09-09.command-ack-reminders.1

這份更新以提供的 `line-translator-bot-main.zip` 為基礎，適用於常駐運行的伺服器。壓縮檔只包含新增及修改的檔案，依原專案的相對路徑覆蓋後重啟服務即可。

## 使用方式

1. 群組輸入 `/ack 今天下班前請完成設備檢查。`，或 `/確認 今天下班前請完成設備檢查。`。
2. 機器人使用既有翻譯流程，送出原文、譯文和作業確認按鈕。單獨輸入 `/ack` 或 `/確認` 會顯示用法；內容最多 1500 個 LINE 字元。
3. 到後台「工廠工具」選擇群組，勾選「自動 @ 提醒尚未回覆的人」，輸入等待分鐘數並儲存。例如填 `30`，代表通知成功送出後等待 30 分鐘。
4. 時間到後，機器人在原群組使用真正的 LINE @ 標記仍未回覆的成員，附上同一筆通知的確認按鈕。

一般文字、圖片、語音及文件翻譯不再自動建立確認卡。舊版「工作相關訊息／所有翻譯」設定會按「指令才建立」解讀，原本已關閉的作業確認仍維持關閉。既有通知在原有效期內仍可回覆，但不會補排新的自動提醒。

在後台「快捷鍵」可調整作業確認按鈕與觸發方式，預覽情境也新增「指令作業確認卡」。關閉底部快捷選單仍可使用 `/ack`；要停用確認功能，請把作業確認觸發方式設為關閉。至少需啟用一個適用於文字的「了解」或「需要說明」按鈕。

## 提醒規則

| 項目 | 行為 |
| --- | --- |
| 預設 | 自動提醒關閉；預填等待時間為 30 分鐘 |
| 等待時間 | 每群組可設定 1～10079 分鐘；從通知獲 LINE 接受後開始計時 |
| 提醒次數 | 每筆通知一輪；超過 20 人分批點名，每人最多列入這一輪一次 |
| 排除對象 | 發起人、已按了解的人、已按需要說明的人，以及有收到離群事件的成員 |
| 名單 | 建立通知時保存成員快照；之後新加入的成員不加入舊通知 |
| 修改時間 | 套用到新通知，既有通知保留原來的期限 |
| 關閉提醒 | 停止尚未派送的提醒；已送出的訊息不會被收回 |
| 全員回覆 | 結束排程，不送提醒 |
| 原文編輯／收回 | 原通知失效，停止後續提醒；需重新用指令發起 |
| 回覆有效期 | 原通知建立後 7 天；了解不代表作業已完成 |
| 查看狀態 | 後台作業確認紀錄會顯示名單範圍、未回覆者、預定提醒時間及派送狀態 |

排程每 15 秒檢查一次；實際發送時間也受待處理筆數、資料庫與 LINE 回應時間影響。這是群組成員主動按鈕回覆的紀錄，並非 LINE 已讀名單。

## 群組名單與 LINE 限制

建立通知時會嘗試取得完整群組成員 ID；LINE 此 API 限認證或尊榮官方帳號使用。權限不足或查詢失敗時，改用機器人已知的成員名單，後台會標示可能不完整。因此無法承諾一般帳號能點名從未與機器人互動的所有潛水成員。[LINE 成員 ID 說明](https://developers.line.biz/en/docs/messaging-api/getting-user-ids/)

單則訊息最多 20 個原生標記，且被標記者必須仍在同一群組。本更新會分批處理，並記錄離群事件；如果機器人漏收到離群事件，LINE 仍可能拒絕該批，後台會顯示失敗原因。[LINE TextV2 與標記規格](https://developers.line.biz/en/reference/messaging-api/nojs/#text-message-v2)

初始確認通知及自動提醒使用 Push API，以持久保存的固定 retry key 避免重試重複；使用量計入 LINE 的推播額度。網路逾時後重試同一批時，保持原訊息與名單不變，避免改變可能已被 LINE 接受的請求；因此在該次嘗試後才按鈕回覆的人，仍可能看見同一批的既有提醒。超過 23 小時仍無法確認的批次會停止重試並顯示待確認。[LINE 重試機制](https://developers.line.biz/en/docs/messaging-api/retrying-api-request/)

## 維運

沿用既有工廠功能儲存設定：有設定 Upstash Redis REST 時使用相同儲存；SQLite 則沿用 `LINE_FACTORY_DB_PATH`。通知與到期索引一起寫入，不需要額外安裝套件。資料庫必須可跨服務重啟保留。

`FACTORY_ACK_WORKER_ENABLED` 預設啟用；未設定時會參考既有的 `REMINDERS_WORKER_ENABLED`。若原本把後者設為 `0`，請將 `FACTORY_ACK_WORKER_ENABLED=1`，讓本功能在常駐服務內執行。後台會顯示是否啟用及最近檢查時間。多個程序透過資料庫的原子更新搶占工作，避免同時重複處理。

這份更新不會替你部署或向正式 LINE 群組發送測試訊息。覆蓋檔案、重啟後，重新整理後台即可載入新介面。

## 驗證範圍

- 實際 SQLite 儲存及排程的 12 項離線測試通過：計時、排除回覆與離群者、重啟恢復、並行搶占、批次處理、回覆競爭、逾時重試、終止條件及超過 200 筆紀錄的到期索引。
- 以訊息模型替身執行真實專案方法，9 項指令／設定檢查通過，包括普通翻譯不自動出卡、中文指令、舊設定遷移、翻譯失敗、底部選單關閉及跨程序讀取最新提醒設定的行為。
- 修改的 Python 與 JavaScript 語法檢查通過；8 段 Redis Lua 腳本通過 Lua 5.4 語法檢查。
- 本次環境缺少 Flask、LINE SDK、pytest、Redis 測試套件及瀏覽器，無法完成完整 SDK／Flask／Redis 整合與瀏覽器操作測試；尚未在正式 LINE 驗證。

可直接執行不需額外套件的排程測試：

```sh
python -m unittest -v test_ack_reminders_offline
```

完整依賴就緒後，可執行隨附的 Flask／SDK／SQLite／Redis 回歸測試：

```sh
FACTORY_ACK_WORKER_ENABLED=0 python run_translation_offline_checks.py test_line_ack_commands.py test_line_factory_features.py test_factory_receipt_flow.py test_unified_quick_reply.py
```

上線確認可先在測試群組將提醒設為 1 分鐘，發起一筆通知，讓一位成員按「了解」，確認只有尚未回覆的成員被標記，再按實際需要調整時間。
