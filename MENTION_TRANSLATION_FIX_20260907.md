# 點名後文字漏譯修正

## 已確認的原因

基準版本：`9fffa966375933bb75818e3aefa21fa8f768fa51`。取得 GitHub 最新檔案後，逐一比對全部 322 個檔案內容；本修正以此版本為基礎。

使用這則訊息重現：

> @小麥（研磨股班長） tolong bukakan komputer tengah

語言偵測正確辨認出印尼文，也能進入翻譯程序。問題在最後的品質檢查使用另一套點名解析，把 `@小麥（研磨股班長） tolong bukakan` 整段誤列為必須原樣保留的人名。因此，即使供應商回傳完整的中文，仍會出現以下拒絕原因：

```text
missing_literal:@小麥（研磨股班長） tolong bukakan
```

該文字被留在重試佇列，未送到 LINE。持續重試也可能被相同規則再次攔截。此結果來自使用替代 AI 回應及 LINE 傳輸的離線重現，不是讀取正式 Render 紀錄後的判定。

原始 6 組重現測試中，完整職稱且名字前後不另加空格的 2 組失敗；修正後 6 組均成功。另發現名字本身含連續空格時，回覆整理程序會壓縮空格，導致人名完整性檢查失敗，本次一起修正。

## 修改內容

- 新增共用的 `translation_mentions.py`。點名擷取、語言辨識前處理、不可變資料保護及譯文人名檢查使用同一套文字邊界。
- 關閉括號的名字／職稱在括號處結束；常見印尼文及英文句子開頭不被併入名字。
- 保留中英混合名字、括號名字、`@All`、多次點名及名字內原有空白；清理重複點名產生的空白時只處理名字之外的部分。
- 更新品質檢查版本識別；既有快取範圍包含此識別，因此不同檢查版本不會直接沿用舊版快取。
- 保留原本模型、AI 呼叫預算、上次加速修改、持久重試與必要的品質檢查。本次未增加 AI 呼叫。

本修正不更動圖片辨識設定。截圖中的圖片是否被關閉、下載失敗、OCR 失敗或傳送失敗，仍須當時的 Render 紀錄才能確認。

## 驗證結果

- 新增 37 項回歸測試，涵蓋 10 種點名格式、有／無 LINE 點名資料、不同語句、重複點名及供應商恢復後的重試。
- 原句通過完整文字處理流程，保留點名並送出中文；每次成功情境只呼叫一次替代翻譯供應商。
- 故意刪除人名、改錯 I01、改錯 5 mm 或保留未翻譯印尼文字的候選仍會被拒絕。
- 完整測試：**1,489 項通過，438 項子測試通過，0 失敗**。
- 工廠資產檢查：通過 30 則標準案例，拒絕 29 則禁止候選，沒有錯誤或警告。

測試沒有呼叫付費 AI，也沒有對正式 LINE 群組發送訊息；這些結果不代表 Render 已部署本修正，或所有線上翻譯都已驗證。

## 上傳方式

ZIP 只有以下 6 個更新或新增檔案，全部放在專案根目錄：

1. `app.py`
2. `translation_quality_gate.py`
3. `translation_mentions.py`
4. `test_mention_translation_delivery.py`
5. `README.md`
6. `MENTION_TRANSLATION_FIX_20260907.md`

一次上傳這 6 個檔案，保留專案其他檔案。務必包含新增的 `translation_mentions.py`；不要只替換 `app.py`。沒有新的環境變數要填。

等待 GitHub 測試成功及 Render 新部署顯示 Live 後，在測試群組實際選取小麥的 LINE 點名，再輸入 `tolong bukakan komputer tengah`。確認機器人回覆中文並保留名字。這句的中文意思可表達為「請幫忙開一下中間那台電腦」。

若新部署後文字仍沒有回覆，請提供該則新訊息時間前後的 Render Logs。若只有圖片仍未翻譯，請提供圖片處理紀錄，特別是 `image_handler_entered`、`image_mode_check`、`image_skipped`、OCR 或 LINE 傳送錯誤附近的行；不要附上 API 金鑰。
