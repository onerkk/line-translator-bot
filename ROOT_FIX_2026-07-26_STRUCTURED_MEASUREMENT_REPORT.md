# 結構化量測回報無法翻譯根治（2026-07-26）

版本：`v3.42.0-structured-measurement-report-root-fix-2026-07-26`

## 截圖訊息

```text
i9
Depan 22,17
Tengah 22,15
Belakang 22,16
Kebulatan ok.
```

正確輸出：

```text
I9
前端：22,17
中間：22,15
後端：22,16
圓度：正常
```

## 根因

這不是語言偵測失敗。程式已能把 `i9` 正規化為設備代碼 `I9`，但後續仍把整份量測表當成一般生成式翻譯。

正式政策對所有中印新生成譯文要求第二次來源複核，且複核失敗時採 fail-closed。這類短表格又沒有既有的來源欄位框架，因此下列任一狀況都會讓最終結果變成 `None`，LINE 只能顯示「暫時無法完成安全翻譯」：

1. 第一個模型或第二次複核暫時不可用；
2. 模型只翻譯欄位名稱，但保留 `OK`，被中文目標語言純度規則判定為未翻譯英文；
3. 模型改動、遺漏任一數值或設備代碼，遭最終驗收封鎖。

截圖本身沒有後端事件紀錄，因此不能斷言當次是哪一個子原因；但共同架構根因可以確定：**可由來源完整驗證的表格，錯誤地依賴兩次生成式模型呼叫。**

## 根治方式

新增 `factory_structured_report.py`，在 cache、TM、NMT、LLM 和強制第二複核之前辨識「設備量測／品質狀態表」。

### 安全條件

只有全部非空白行都符合以下欄位時才啟用：

- 可選的單一設備代碼，例如 `I9`、`Mesin I9`；
- 至少兩個位置量測欄位，例如 `Depan`、`Tengah`、`Belakang`；
- 至少一個品質欄位，例如 `Kebulatan`、`Kelurusan`、`Diameter`；
- 值必須是數字／單位，或明確的 `OK/NG/normal/lulus/gagal` 狀態；
- 不允許未知行、重複欄位或不完整報告。

符合時直接從目前來源欄位重建繁體中文，不呼叫任何 AI；不符合時完全不猜測，繼續原本翻譯管線。

### 資料完整性

- 數值與小數逗號原樣保留；
- 設備代碼先使用現有設備正規化器處理；
- `OK` 依品質欄位語義轉成「正常」，`NG` 轉成「異常」；
- 仍通過現有 `_final_delivery_guard`；
- 只有驗收成功後才寫入 cache，避免髒快取。

## 驗證

- 截圖原句實際匯入 `app.py` 測試：成功走 `deterministic_structured_measurement_report`，`openai_status=not_needed`。
- 新增 6 個獨立回歸測試，涵蓋小數逗號／小數點、單位、欄位別名、OK／NG、未知行、不完整與重複欄位。
- 離線發布驗證：18/18 核准案例通過，18/18 禁用探針被拒絕，零錯誤、零警告。
- 依賴無關回歸測試：114 項全部通過。
