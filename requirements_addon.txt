# v3.9.41 業界 COMPLETE 整合 — 依賴清單
# 把這些加到 Render 的 requirements.txt 重新部署

# Phase A 必要
rapidfuzz>=3.5.0

# 其他既有依賴(已在原 requirements.txt 內,確認有):
# openai>=1.0     # Phase B/D/E/K/L/Q
# anthropic>=0.40 # Phase D/E/K/Q

# === 環境變數 ===
# OPENAI_API_KEY=...         必要 (B/D/E/K/L/Q)
# ANTHROPIC_API_KEY=...      必要 (D/E/K/Q)
# GOOGLE_TRANSLATE_API_KEY=  可選 (C,沒設則跳過 NMT)
# TM_DB_PATH                 預設 /var/data/translation_memory.db
# VECTOR_TM_DB_PATH          預設 /var/data/vector_tm.db
# ACTIVE_LEARNING_DB_PATH    預設 /var/data/active_learning.db

# === SQLite DB(自動建立)===
# /var/data/translation_memory.db   (Phase A)
# /var/data/vector_tm.db            (Phase B)
# /var/data/active_learning.db      (Phase L)

# Translation delivery availability (optional but recommended)
# LOCAL_TRANSLATE_URL=http://127.0.0.1:5000/translate
#   LibreTranslate-compatible local endpoint used before public fallbacks.
# TRANSLATION_RETRY_DB_PATH=/var/data/translation_retry_queue.db
# TRANSLATION_RETRY_MAX_DELAY=300
# Optional fully offline route: install argostranslate and preinstall the needed
# zh/id/en language packages. The application never downloads models at runtime.
