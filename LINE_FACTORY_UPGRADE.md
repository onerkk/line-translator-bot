# LINE 工廠翻譯機器人整合更新 — 2026-09-07

本包只包含修改或新增的檔案。比對基準是 `onerkk/line-translator-bot` 的 `32ddc95b980c77bdc2563a0c6b6df0358945856b`，不是最初上傳的舊 ZIP。請將整包解壓覆蓋目前專案根目錄，保留 `static/`、`templates/`、`tests/` 及 `.github/` 的目錄結構；不要只上傳 `app.py`。

此版本已完成程式整合及離線驗證，尚未替你部署到 Render，也沒有使用你的帳號發送 LINE 訊息或呼叫付費 AI。

## 新功能與入口

| 功能 | 操作方式 | 實際行為 |
| --- | --- | --- |
| 原文修改後重譯 | 在 LINE 修改已傳送的文字 | 收到官方 `messageEdited` 事件後，停止尚未完成的舊工作，補發標示「原文已修改」的新譯文；過期按鈕不能再使用舊版本 |
| 原生 LINE 點名 | 原文透過 LINE 正式選取人員 `@` | 依 webhook 的人員識別資料保留可點擊的點名；同名人員依出現順序處理；LINE 拒絕該點名時保留可讀姓名再送出 |
| 只在被點名時翻譯 | 後台「工廠工具」→選群組→文字翻譯模式 | 預設仍自動翻譯所有文字；可改成只有真正 `@機器人` 時翻譯，圖片、語音、文件及管理指令維持各自規則 |
| QR／設備／站別翻譯 | 群組輸入 `/factory`，開啟工具後掃碼或輸入代碼 | 查閱設備雙語名稱、背景、核准作業說明及相關表單；可依所選設備背景翻譯短句 |
| 雙語公告分享 | 譯文的分享入口，或工廠工具的分享按鈕 | 預覽原文與譯文，由使用者開啟官方分享選擇器並選收件人；取消不會顯示成成功；內容過長可完整複製 |
| 作業確認 | 譯文下方「已了解／需要說明」，後台查看紀錄 | 依 LINE 使用者分別記錄回覆。預設工作相關訊息顯示，可改成全部或關閉；未回覆名單只涵蓋機器人已知成員 |
| 圖文選單使用統計 | 後台「工廠工具」→圖文選單使用統計 | 輸入 rich menu ID、日期，查詢官方期間或每日顯示／點擊資料；快取與查詢上限避免反覆請求 |

`/工廠`、`/qr`、`/掃碼` 也可開啟工具。既有翻譯、OCR、語音、文件、翻譯回饋、提醒與表單保留；新增功能接入既有的翻譯驗證與送出流程。

作業確認是同事主動點按的紀錄，不能當成 LINE 個別成員已讀資料。LINE API 也不能修改或收回機器人已送出的舊譯文，因此更正會另發一則；已被 LINE 接收的在途訊息，無法保證在原文修改瞬間撤回。

## 設備、站別與 PMI 資料

工具直接讀取原有 `STATION_CODES` 與 `STATION_NAMES`，包含設備代碼、中文站名與數字站別，例如 `E1-1`、`UT手`、`UT自`、`480`、`801`；也保留舊詞庫中可辨識的代碼對照。

- `I5`、`I15` 在原資料存在研磨與拋光的重複用途。沒有額外資訊時會顯示「需確認製程」，不能只憑代碼決定。管理員可在特定群組建立明確對照；群組對照優先於全廠對照及內建資料，代碼大小寫視為同一個代碼。
- 同一數字站別可能有多種名稱／用途，例如 `490`。查詢保留原資料的多種對照，不能任選第一個就套用到所有訊息。
- `PMI` 背景固定為鋼種檢驗的行為流程，避免理解成檢查儀器本身或列印標籤。原有 PMI 語義檢查仍會執行。
- 原資料只有設備名稱時，作業說明保持空白。請在後台填入實際核准的中文、印尼文 SOP；系統不會編造操作流程。
- 所選設備背景只協助判斷原文簡稱，不會把整份 SOP 加進譯文，也不得覆寫原文明確寫出的設備、動作、數字或否定。
- QR Code 只包含 `line-factory:代碼`，不包含 API 金鑰或工廠 SOP；掃碼後仍需有效的工具入口。

## 一次完成部署與 LINE 設定

### 1. 更新專案並部署

把本包全部檔案依原路徑上傳到目前 GitHub 專案，再到 Render 的 `line-translator-bot` 服務確認部署成功。本版需要重新安裝 `requirements.txt`，包含支援新事件／訊息格式的 LINE SDK 與 QR 產生套件。

本包沒有金鑰、資料庫或你的執行中設定。請保留現有環境變數，不要用舊金鑰覆蓋已經測試成功的新金鑰。

### 2. 檢查 Render 環境變數

| 名稱 | 填入內容 |
| --- | --- |
| `LIFF_ID` | 現有 LINE Login channel 內的 LIFF App ID |
| `LINE_LOGIN_CHANNEL_ID` | **該 LINE Login channel 的 Channel ID**，不是 Messaging API channel 的 ID；表單伺服器會以此驗證實際 LINE 使用者 |
| `OPENAI_API_KEY` | 目前有效且有模型請求權限的金鑰；在 Render 私下填入。若後台更新的金鑰與環境變數不同，AI 頁會提示差異 |
| `UPSTASH_REDIS_REST_URL`、`UPSTASH_REDIS_REST_TOKEN` | 有既有 Upstash 時繼續使用，互動紀錄會自動沿用，不需另建服務 |

LINE Login 與 Messaging API channel 應在同一個 LINE Provider 下，才能讓兩邊的使用者 ID 對應。管理頁「工廠工具」會顯示 LIFF ID、表單身分驗證、儲存與 AI 設定狀態；「已設定」不代表已完成線上呼叫驗證。

### 3. 設定既有 LIFF App

開啟 [LINE Developers Console](https://developers.line.biz/console/)，選擇目前的 Provider → LINE Login channel → LIFF → 目前的 LIFF App。

1. Endpoint URL 設為 `https://line-translator-bot-9khl.onrender.com/liff/settings`；如果服務網址已改，用實際網址替換網域。
2. Size 設為 **Full**。
3. Scopes 保留既有需要的項目，並啟用 **profile**。
4. 開啟 **Scan QR** 與 **Share target picker**；分享功能若出現官方條款畫面，由帳號管理者確認。
5. 儲存。若要提供一般同事使用，確認 LINE Login channel 已發布，而非只限開發者使用。

本版會先執行 `liff.init()` 再處理官方的 `liff.state` 導向，讓同一個 LIFF 入口可進入既有設定、表單與新工廠工具。掃碼仍受手機、LINE 版本及相機權限影響；不支援時可手動輸入設備代碼。

表單使用伺服器驗證過的 LINE access token，不採信前端任意填寫的使用者 ID。限定群組的表單只開放已知成員，或持有自己在該群組取得的有效工具入口者；不能使用別人的入口冒名填表。

### 4. 設定群組與設備

登入原管理後台 →「工廠工具」→選擇群組。先保留「自動翻譯所有文字」，確認原本長通知正常翻譯後，再按需求改成點名模式。委派管理者若看不到新頁籤，需由主管在既有權限管理開放「工廠工具」。

找到設備並點「編輯」，補上該群組的正式名稱、背景、雙語作業說明及開放中的表單，再儲存。按「QR Code」可下載並列印貼在設備旁；表單若限定群組，設備也必須指定相容群組。

### 5. 確認服務儲存方式

互動紀錄有 Upstash 時使用 Upstash；沒有時使用本機 SQLite。翻譯重試佇列及本版的來源版本紀錄仍是本機 SQLite。

Render 免費主機會休眠，重新部署也可能清除本機資料；本版不能靠程式消除主機冷啟動時間或保證免費主機的佇列跨部署保留。既有資料庫快照僅涵蓋原來的翻譯記憶／學習資料，**不包含翻譯重試佇列**。

若部署方案已有持久磁碟並掛載在 `/var/data`，可設定：

```text
TRANSLATION_RETRY_DB_PATH=/var/data/translation_retry.db
LINE_FACTORY_DB_PATH=/var/data/line_factory.db
```

這些路徑不會自行建立 Render 持久磁碟。來源版本紀錄會放在重試資料庫旁；既有資料移到新路徑前應保留備份。本版沿用目前單服務實例的部署方式，沒有加入多實例佇列協調。

## 一併修正的既有問題

- 翻譯送出失敗改走重試時，保存完整訊息、Flex、附件、引用及按鈕，避免只剩純文字或漏掉第六則以後的附件。每批送出都有檢查點及穩定重試識別。
- 原文修改／收回會撤銷舊工作租約，停止舊版文字、圖片、語音、文件與按鈕衍生工作的後續送出；重複 webhook／按鈕事件不重複執行。
- 互動紀錄服務暫時失效時，核心翻譯可繼續送出；不會因新功能無法儲存而把整則譯文吞掉。
- AI 測試只測試所選供應商，區分沒有金鑰、額度被停用及暫時連線停用；空結果不顯示成功。有效測試可解除舊的額度停用紀錄。
- 金鑰更新採原子儲存，失敗回復原設定；語音辨識／朗讀改用最新執行中金鑰，不再卡在啟動時的舊 client。選擇其他文字供應商不會無故停用已設定的 OpenAI 語音功能。
- 修正既有表單清單狀態、必填／選項／數字／日期檢查、重複送出與儲存失敗處理；表單需驗證 LINE 身分才能提交。
- 修正 LIFF 初次導向、設定頁 LIFF ID 注入、過期 nonce 被舊磁碟資料復活，以及 AI 頁模型清單的錯誤變數。

## 速度、費用與資料期限

掃碼查對照、查看已填 SOP、作業確認、產生 QR 與整理分享內容不呼叫翻譯 AI。站別翻譯沿用原翻譯器，成功結果以使用者、群組、設備、原文、語言及資料版本隔離快取 10 分鐘；新翻譯請求每人每分鐘最多 20 次。失敗結果不當成成功譯文快取。

LINE 訊息費用仍依官方帳號方案及送出方式計算，AI 費用依實際模型與用量計算；本版未改成最貴模型，也未宣稱所有情境都已達到最低價格或最低延遲。

工具入口有效期 10 分鐘；逾期可在群組重新輸入 `/factory`。翻譯互動按鈕另受原來源紀錄期限及修改／收回狀態限制，作業確認紀錄最多保留 7 天。共用工具連結具有存取能力，表單仍會額外核對實際登入者。

圖文選單統計使用日本時間 UTC+9。官方尚未完成彙整或未達隱私門檻時會顯示「未提供」，不能解讀成零次。查詢資料快取 10 分鐘，並保留官方頻率限制的餘裕。

## 驗證結果與部署後確認

此次完整離線測試：**1,192 項測試、438 項子測試通過**。正式工廠資料驗證通過，含 30 個正式回歸案例與 29 個禁止譯法探針；原本案例、詞庫與工廠語義檢查保留。

測試使用真實 LINE SDK 資料模型、Flask 簽章 webhook、SQLite 與模擬 Redis 執行 Lua，外部 LINE／AI 傳輸使用測試替身。新增 DOM 整合測試實際執行頁面 JavaScript，涵蓋群組設定、設備 SOP／QR、確認紀錄、每日統計、掃碼失敗、翻譯、複製、分享成功／取消，以及表單驗證／送出。

沒有完成實機 LINE／相機、實際付費 AI 或手機畫面視覺驗收。離線測試也不能證明未知句子永遠翻譯正確。部署後用以下五組操作確認實際帳號設定：

1. AI 頁測試成功後，在原群組重送包含五點及 PMI 的長通知，再測圖片、語音、文件各一則。
2. 修改其中一則文字，確認另收到更正譯文；測一次點名模式，再恢復需要的模式。
3. 輸入 `/factory`，查 `E1-1`、`UT手`、`I5` 與 `PMI`，下載一張 QR 用手機掃描，確認資料及群組對照。
4. 開啟設備表單填寫、按一次「已了解」，到後台確認同一個使用者的紀錄；在分享選擇器先取消，再自行選測試對象分享。
5. 用正在使用的 rich menu ID 查詢過去日期；未達門檻時應顯示未提供，而非錯誤或零次。

離線重跑：

```bash
python -m pip install -r requirements.txt -r requirements-test.txt
python validate_factory_translation_assets.py --json
python -m pytest -q test_*.py
```

請在沒有正式 API 金鑰的測試環境執行；GitHub release gate 已設定離線假 LINE 憑證，另包含 JS 語法與 DOM 整合測試。DOM 測試需要 Node.js 24 及 `jsdom@30.0.1`，以 `JSDOM_PATH` 指向安裝目錄後執行 `python tests/run_factory_ui.py`。

如需回復程式版本，可由上述 Git commit 還原本包修改過的舊檔；還原前保留新版本設定／互動資料備份。不要為回復程式而刪除雲端資料或已有效的 API 金鑰。

## 官方依據

- [Messaging API 參考](https://developers.line.biz/en/reference/messaging-api/)：編輯事件、textV2 點名、訊息限制、圖文選單統計。
- [LIFF API 參考](https://developers.line.biz/en/reference/liff/)：`scanCodeV2`、`shareTargetPicker` 與相容性條件。
- [LIFF 初次與二次導向](https://developers.line.biz/en/docs/liff/opening-liff-app/)：`liff.state` 與初始化順序。
- [伺服器驗證 LINE 使用者](https://developers.line.biz/en/docs/liff/using-user-profile/)：access token 驗證與 profile 取得方式。
