# ACK118：已知未回覆為 0，自動停止提醒

本次更新接續 roster117，僅包含本次修改檔案。

## 新行為

- 已知成員未回覆為 0，立即停止該通知的提醒，不再以「名單尚未完整」繼續提醒全體。
- 最後一位已知待回覆成員按「了解」時，同一筆儲存操作會記錄回覆並清除下一輪排程、待重試提醒及工作鎖。
- 仍有已知成員未回覆時，保留設定的提醒間隔、持續提醒與手動停止功能。
- 發起人、本機器人及已離群者不列入待回覆。歷史「需說明」回覆仍視為已回覆；不新增「不了解」按鈕。
- 確認卡及後台顯示「已自動停止提醒」，完成後的最新卡片與後台隱藏停止按鈕，保留了解功能。
- 已知名單一開始為空，仍正常發送原始確認卡，送達後停止後續提醒。原始卡片若送達不明，仍使用相同識別碼重試。
- 停止後，即使之後辨識到新成員或重啟服務，也不會重啟該通知的提醒。

## 既有通知

不必重新發起通知。更新後再按了解、查看回覆，或開啟後台確認紀錄時，會依新規則處理既有通知。沒有任何操作的舊通知，會在下次原訂排程檢查時停止，且不再發送零人待回覆的提醒。

LINE 已接收或已開始傳送的那一筆請求無法撤回；最後回覆之後不會再安排下一輪或重試。

## 套用

1. 保留 ZIP 內的資料夾路徑，將全部檔案覆蓋到目前專案根目錄。
2. `line_ack_reminders.py` 與 `line_factory_features.py` 必須一起更新，兩者版本皆為 `2026-09-09.ack-known-zero-stop.4`。
3. `static/admin_factory.js` 必須放在 `static/`；`tests/` 內的測試必須放回 `tests/`。`app.py` 僅更新此後台檔案的瀏覽器快取版本。
4. 提交後確認 CI 通過，再依目前服務的部署流程重啟或重新部署；後台重新整理一次。

本 ZIP 不是完整專案，不包含金鑰、資料庫或群組設定，也不修改既有群組的設定值。不要在套用本次更新後執行舊版更新安裝器，以免覆蓋回舊版核心。

## 驗證

- 全部離線回歸：2053 項通過、443 個子測試通過。
- 本次新增停止情境：30 項通過，涵蓋 SQLite 與 Redis 儲存介面。
- 真實 DOM 介面測試全部通過，包含最後回覆後立即停排程及後台狀態更新。
- 前端版本檢查、JavaScript 語法及翻譯資產驗證通過。

測試使用隔離資料及模擬 LINE 傳送，未向正式群組發訊息，未修改線上伺服器。
