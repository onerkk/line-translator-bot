# 印尼文「Mesin」被翻成工單欄位名稱：根治修正

## 實際根因

錯誤不是三家 AI 都不懂 `Mesin ya kebakar`，而是共用的翻譯記憶庫（TM）在啟動時，把中→印詞庫錯誤反轉後寫入：

- 正向詞庫：`工單製程紀錄「機台」 → Mesin`
- 舊版反向種子：`Mesin → 工單製程紀錄「機台」`

`Mesin` 是一般印尼文「機台／機器」，不能反推成完整的工單欄位路徑。因為 TM exact match 會優先於 OpenAI、Claude、Gemini，所以三家 AI 都可能被共用的錯誤快取繞過。

## 架構修正

1. 反向詞庫與 prompt 注入統一使用 `build_safe_reverse_index()`：
   - 一般單字不自動反轉。
   - 重複或多義對照不反轉。
   - 含 `「」`、`【】` 等 UI／欄位結構的詞條，除非明確標記 `reverse_safe=true`，否則不反轉。
2. `glossary_seed` 改成「衍生資料同步」：每次啟動先刪除舊種子，再從目前詞庫重建，避免舊錯誤永久殘留。
3. 啟動時清除 Lexical TM 與 Vector TM 中，目標文字等於不安全 UI 欄位名稱的歷史污染資料。
4. TM／Vector TM 提前回傳前，加入資料驅動的 UI 欄位洩漏檢查。
5. LINE 最終送出前再次檢查；若命中污染結果，直接從原文重新翻譯，不對錯譯做字串補丁。
6. 新增設備故障語義槽位：
   - `kebakar`／`terbakar` → `著火了`
   - `Mesin ya kebakar,` → `機台著火了`
   這條在 AI provider 前執行，因此 OpenAI、Claude、Gemini 三條路徑共用同一結果。

## 驗證

- `Mesin` 不再進入安全反向索引。
- `工單製程紀錄「機台」` 會被辨識為反向詞庫 UI 欄位洩漏。
- `Mesin ya kebakar,` 與 `Mesinnya terbakar` 均輸出 `機台著火了`。
- 舊 `glossary_seed` 與其他已污染 TM 目標可被清除。
- 全部 18 項自動測試通過。
