# 2026-09-11 翻譯語義與自動學習更新

本包是本次上傳 `line-translator-bot-main.zip` 的變更檔。解壓後，將檔案放在原專案根目錄，覆蓋同名檔並部署。六個執行檔須一起更新，包含新檔 `translation_learning_policy.py`。不需要新增套件或重新貼教學範例。

## 截圖核對

| 原文 | 核對結果與建議譯文 |
| --- | --- |
| 支數2支 | `Jumlah batang: 2 batang` 正確，保留既有本機報表翻譯，零次翻譯 API。 |
| 支數入庫前檢查一下再按，這個很麻煩，但是真的要做 | 原譯大意可理解，但 `memasukkan jumlah batang ke gudang` 把數量寫成送進實體倉庫，系統登錄語境不清楚。較清楚的譯法：`Sebelum menekan tombol untuk mencatat jumlah batang dalam sistem, periksa sekali lagi. Ini memang merepotkan, tetapi benar-benar harus dilakukan.` |
| 支數少的稍微確認一下 | 原譯 `Untuk jumlah batang yang sedikit, periksa sebentar.` 大意成立，屬自然度問題，不應當成嚴重錯誤阻擋。建議：`Jika jumlah batangnya sedikit, tolong diperiksa.` |

截圖只有分鐘刻度，不能據此推算每句確切翻譯耗時。

## 找到並修正的根因

1. **資料操作缺少共用語義檢查。** 原有輸入語義主要處理手動輸入與磅秤取值；對「欄位／數量＋入庫／輸入＋檢查順序」沒有完整約束。本次用欄位、操作及時間連接詞建立語義，供提示詞、供應商候選檢查、最終交付及學習校驗共同使用。實體搬運不被改成資料登錄，前後順序可雙向核對。「稍微／一下」只給自然語氣指引，不硬性要求某一種措辭。
2. **供應商切換把失敗證據丟掉了。** 舊流程在拒絕首個候選後直接切換，後面的學習階段可能只看到成功結果。本次保留每則請求最多兩個被拒絕的候選，成功恢復後才重查它們能否成為學習證據；下一則訊息會清空候選，避免串訊息。
3. **舊學習主要增加複核，沒有在首輪預防錯誤。** 本次將已確認的錯誤分類與原文語義特徵存成規則，在新句子的第一次請求中選取相關提醒。規則描述要檢查的語義關係，不複製舊答案、數字、人物或指令方向。
4. **已知本機約束不必永遠重複付費複核。** 只有當歷史錯誤已學到、且本次來源仍啟用對應的明確本機檢查時，才取消單純因歷史風險造成的額外複核。當前候選若未通過檢查，仍可修正；未知問題、重大事件及未解決的語境，保留原有複核策略。
5. **學習儲存不應拖住回覆。** 學習寫入、規則讀取與風險查詢的 SQLite 鎖等待限制為 50 毫秒；繁忙或失敗時略過此次學習，不拖成十秒等待。學習模組的資料庫連線也會在提交／回滾後關閉。

直接從原 ZIP 載入舊輸入語義模組重現：它會接受上述實體倉庫式譯法，也會接受「先輸入再檢查」的反向譯法。更新後這兩個問題分別被資料登錄及順序檢查檢出。

## 如何持續學習

- 成功修正後，重新用當前校驗器檢查舊譯與新譯。必須舊譯確實失敗、新譯通過，才提煉規則；只換措辭、自報信心、假稱已修正或不合格的新答案，都不能教出規則。
- 從錯誤類型與原文特徵找相似情境，不要求新句子逐字一致。保留既有人工核准案例的詞彙／語義檢索、明確數量模板及版本化快取。
- 新規則不保存可直接回覆的 AI 目標譯文，也不會自動變成人工核准案例。相似度只用來選提醒，不能當成直接套用譯文的依據。
- 按群組及語言方向隔離；最多選四類規則，規則提示上限 1,200 字元。重複重試不冒充多個獨立證據；規則索引最多保留 3,000 筆。
- 人工修正被撤回或隔離後，相關規則不再使用。校驗版本變更後，舊規則不直接沿用。
- 啟動後用既有背景執行緒分頁重查歷史稽核記錄，保存回放進度；每次啟動最多使用 20 秒，未完成的歷史在後續啟動續跑。新發生的合格修正會直接學習，不用等下一次啟動。
- 後台既有 Active Learning 卡顯示規則類別數與證據筆數；`/admin/debug/last-translate` 的 `semantic_contract.learned_policy.rules` 可核對該次選中的規則。這些數字不是正確率。

這是可持續累積的翻譯記憶與錯誤預防策略，不是自動訓練 Claude／GPT／Gemini 的模型權重。系統只能從實際被檢出並通過修正驗證的問題學習；校驗器和模型都未辨識出的新錯誤，不能保證自動發現。

## 驗證

- 完整、全新測試資料庫的離線回歸：**2,746 項測試、443 個子測試通過**；本次新增 52 項。整套測試耗時 108.76 秒，並非 LINE 翻譯時間。
- 既有後台、會員頁、LIFF 表單、指定對象確認、提醒、統一選單的真實 DOM 操作測試通過。
- `validate_factory_translation_assets.py --json` 通過，無錯誤或警告；部署版本與前端資產檢查通過。

新增的主流程測試使用真實 `app.translate()`、校驗、SQLite、提示詞編譯與供應商切換邏輯，僅將外部傳輸替換成模擬回應：

| 情境 | 已驗證的流程 |
| --- | --- |
| 第一次返回有缺陷的譯文，第二家返回合格譯文 | 兩次生成後完成，自動記錄可用規則。 |
| 改成未見過的「數量」說法，返回合格譯文 | 規則實際送進首輪提示詞；只新增一次生成，不強制再複核。 |
| 相同來源再次出現 | 使用既有已驗證快取，不新增生成。 |
| 所有模擬供應商都返回有缺陷結果 | 不將失敗修正學成規則。 |
| 「支數2支」 | 本機報表翻譯，零次生成。 |
| 「支數少的稍微確認一下」 | 首輪收到自然語氣指引；合格結果只用一次生成。 |

模擬回應按測試腳本提供，因此以上證明的是編排、驗證與 API 呼叫控制，不能當成真實模型準確率提升的量測。

本機規則檢索壓力測量：300 個合成來源的索引負載、300 次獨立查詢，SQLite 選取與提示詞組裝的中位數 **2.512 毫秒**、P95 **3.078 毫秒**；該案例規則提示 **351 字元**，翻譯／embedding API 呼叫皆為 **0**。可用 `python benchmark_learned_policy.py` 重跑；這不是 LINE 或模型端的實際延遲。

本次沒有部署到正式 Render，也沒有使用你的正式 AI 額度或讀取正式 Logs；不聲稱任何句子都百分之百正確、秒回，或保證固定節省百分比。

## 儲存與官方依據

學習規則沿用 `active_learning.db`，既有資料庫快照會涵蓋新表。若正式環境尚未使用持久磁碟或既有 Upstash 快照，Render 免費服務的暫存磁碟無法保證跨休眠／重啟保存；免費服務喚醒也會增加等待。這些主機限制無法靠翻譯程式消除。[Render 免費服務說明](https://render.com/docs/free)

本次採用減少串行外部請求、只送相關規則、固定原則在前與動態資料在後的方式，與官方延遲最佳化原則一致。[OpenAI 延遲最佳化](https://developers.openai.com/api/docs/guides/latency-optimization)

供應商的 Prompt Caching 有長度門檻，不能僅憑設定已開啟就宣稱每句命中或固定省多少錢；實際仍要核對 usage。既有供應商快取與用量計算沿用原程式。[Claude Prompt Caching](https://platform.claude.com/docs/en/build-with-claude/prompt-caching)

本次用來源事實、相反語義、陌生說法及真實流程測試建立回歸驗證，區分離線流程檢查與正式模型表現。[OpenAI 評估文件](https://developers.openai.com/api/docs/guides/evals)

## 檔案

| 檔案 | 用途 |
| --- | --- |
| app.py | 主流程接入學習、保留被拒絕候選、複核控制、背景回放與後台統計。 |
| active_learning.py | 學習證據校驗、持久化、歷史回放及連線／等待管理。 |
| translation_learning_policy.py | 新增：從已驗證錯誤提煉、檢索及編譯通用規則。 |
| factory_input_semantics.py | 欄位登錄與實體操作、檢查順序、自然請求語氣。 |
| prompt_optimizer.py | 編譯時保留有界學習規則。 |
| translation_quality_gate.py | 更新品質版本，避免舊快取繞過新檢查。 |
| test_translation_learned_policy.py | 新增的 52 項回歸測試。 |
| benchmark_learned_policy.py | 可重跑的本機規則延遲測量。 |
| UPDATE_20260911.md | 本說明。 |

基底 ZIP SHA-256：`c76079df28bedb3020e24e1a4e7626c8e30c1c436c249e200a32ca472e264c0a`。
